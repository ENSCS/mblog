# mBlog — สิ่งที่สร้างไปแล้ว (ใช้งานได้จริงตอนนี้)

รายละเอียดระดับไฟล์/ฟังก์ชัน/บั๊กที่เจอและแก้แล้วของทุกฟีเจอร์ที่ mBlog มีอยู่จริง แยกออกมาจาก [PLANNING.md](PLANNING.md) เพื่อให้ไฟล์นั้นอ่านเร็ว/เบา — เปิดไฟล์นี้เมื่อต้องรู้รายละเอียดเชิงลึกของฟีเจอร์ใดฟีเจอร์หนึ่งเท่านั้น ไม่ใช่ทุกครั้งที่เริ่มงานใหม่

---

ระบบเขียนบทความแบบ WYSIWYG พร้อม **MySQL เต็มระบบแล้ว** (ย้ายออกจากไฟล์ json/array เดิมทั้งหมด — รายละเอียดการย้ายดู Phase 1 ใน [PLANNING.md](PLANNING.md) หัวข้อ Roadmap):

- **โครงสร้างไฟล์**
  - `index.php` — **แค่ 302 redirect ไป `articles.php`** (ไม่ใช่ 301 — เป็นการจัดวางชั่วคราว วันหน้าจะเอา `index.php` ไปทำหน้าแรกจริงๆ ก็ลบ redirect นี้ได้ทันทีโดยไม่ทิ้งร่องรอยกับ browser/Google)
  - `articles.php`, `pages.php`, `category.php`, `tag.php`, `drafts.php` — 5 หน้ารายการที่หน้าตา/พฤติกรรมคล้ายกันมาก (การ์ด + badge + pagination) **รวมให้ใช้ query กลางและ partial กลางตัวเดียวแล้ว** แทนที่จะแยกกันคนละชุดโค้ด: `getArticleList($filters, $page, $perPage)` ใน `includes/articles.php` (filter `type`/`status`/`category_slug`/`tag_slug`) ↔ `partials/article-list.php` (render การ์ด+badge+pagination, ควบคุมด้วยตัวแปร `$showCategoryBadge`/`$showStatusBadge`/`$linkToView` ต่อหน้า) — **แบ่งหน้าแล้วครบทั้ง 5 หน้า** (เดิมมีแค่ `articles.php`) ใช้ค่าเดียวกันจาก `settings.php` (`articles_per_page`, ค่าเริ่มต้น 10) — `sitemap.php`/`admin.php` ยังเรียกผ่าน wrapper เดิม (`getArticles()`/`getPages()`/`getDraftArticles()`) ที่ตอนนี้ห่อ `getArticleList()` ไว้ชั้นเดียว ไม่ต้องแก้โค้ดฝั่งนั้น
    - `articles.php` — รายการบทความ (`type='post'` เฉพาะที่เผยแพร่แล้ว) แยกออกมาจาก `index.php` โดยเจตนา กันบทความหายไปด้วยถ้าวันหน้า `index.php` ถูกเปลี่ยนไปทำอย่างอื่น — badge หมวดหมู่คลิกไปหน้า `category.php` ได้
    - `category.php` — หน้ารวมบทความตามหมวดหมู่ (`?slug=`) อัตโนมัติ ไม่ต้องมาเพิ่มเมนู/หน้าเองทุกครั้งที่มีบทความใหม่เข้าหมวด — 404 ถ้าหมวดไม่มีจริง — ตอนนี้โชว์ badge หมวดหมู่บนการ์ดเหมือน `articles.php` แล้ว (เดิมไม่มี)
    - `tag.php` — หน้ารวมบทความตามแท็ก (`?slug=`) — โชว์ badge หมวดหมู่ด้วยเหมือนกัน (มีประโยชน์กว่ากับ `category.php` เพราะบทความในแท็กเดียวกันมาจากคนละหมวดได้)
    - `pages.php` — หน้ารวม Page ทั้งหมดที่เผยแพร่แล้ว (คู่ขนานกับ `articles.php` แต่สำหรับ `type='page'`)
    - `drafts.php` — รายการร่าง (ล็อกอินก่อนเข้าแล้ว `requireCapability('edit_articles')` — แต่ยังไม่กรอง "เฉพาะร่างของตัวเอง" ต่างจาก `manage-articles.php`/`manage-pages.php` ที่กรองผ่าน `articleOwnerFilter()` แล้วจริง — ดู [PLANNING.md](PLANNING.md) หัวข้อ "ประเด็นที่วนกลับมาซ้ำๆ")
  - `editor.php` — หน้าเขียน/แก้ไข (ใช้ [Quill.js](https://quilljs.com)) แยกปุ่ม "บันทึกร่าง"/"เผยแพร่" + **แก้ slug เองได้** (ไม่กรอกจะสลักจากชื่อบทความให้อัตโนมัติ, เตือนถ้าแก้ slug ของบทความที่เผยแพร่แล้ว)
  - `article.php` — หน้าอ่านบทความ (`type='post'` เฉพาะที่เผยแพร่แล้ว) + **301 redirect อัตโนมัติ** ถ้ามีคนเข้าด้วย slug เก่าที่เคยเปลี่ยนไปแล้ว
  - `page.php` — หน้าอ่าน "Page" (`type='page'` เช่น เกี่ยวกับเรา/ติดต่อเรา/นโยบายความเป็นส่วนตัว — เผยแพร่จริงแล้วทั้ง 3 หน้า พร้อมเนื้อหา mockup) คู่ขนานกับ `article.php` แต่ไม่มี badge หมวดหมู่/OG type เป็น `website` — ดู [PLANNING.md](PLANNING.md) หัวข้อ 1 "Post vs Page"
  - `api/save.php` — บันทึกบทความ/หน้าลง MySQL (คอลัมน์ `type` แยก post/page, หน้าไม่มีหมวดหมู่), sync รูปที่ใช้จริงเข้าตาราง `mblog_images`, บันทึก redirect อัตโนมัติถ้า slug เปลี่ยน — ระบุ "กำลังแก้บทความไหน" ด้วย `id` (ไม่ใช่ `slug` เพราะแก้ slug เองได้แล้ว)
  - `api/upload.php` — อัปโหลดรูป เก็บชื่อไฟล์เดิม (sanitize แบบ WP รองรับทุกภาษา ไม่สุ่มชื่อทิ้งเหมือนเดิม) แยกโฟลเดอร์ตามเดือน `uploads/YYYY/MM/`
  - `includes/articles.php` — ฟังก์ชันกลางอ่าน/เขียนบทความ+หน้าทั้งหมด — แกนกลางคือ `getArticleList($filters, $page, $perPage)` (query เดียวรองรับ `type`/`status`/`category_slug`/`tag_slug` + pagination, ใช้จริงโดย 5 หน้ารายการ) ส่วน `getArticles()`, `getPages()`, `getDraftArticles()` เป็น wrapper บางๆ เรียกมันซ้ำ (ให้ `sitemap.php`/`admin.php` เรียกแบบเดิมไม่แบ่งหน้าได้) นอกนั้นมี `getArticle()`, `getPage()`, `articleViewUrl()` (คืน URL `article.php`/`page.php` ตาม `type`), `getCategoryBySlug()`, `articleCategory()`/`articleCategorySlug()`/`articleCategoryColor()` (คืน `null` เมื่อไม่มีหมวด), `getArticleForEdit()`, `getArticleById()`, `getCategories()`, `syncArticleImages()`, `sanitizeSlug()`/`uniqueSlug()`, `recordSlugRedirect()`/`findRedirectSlug()`, `articleContentPreview()` (พารากราฟแรกของเนื้อหาจริง ตัด HTML ทิ้ง จำกัด 500 ตัวอักษรที่ขอบคำ — ใช้แสดงในการ์ดของ `partials/article-list.php`, คนละตัวกับ `articleExcerpt()` ที่มีไว้ทำ meta description), `searchArticles()` (ค้นหาแบบ `LIKE`, ดู [PLANNING.md](PLANNING.md) หัวข้อ 2), `relativeTimeTag()` (แสดงเวลาแบบ "2 ชั่วโมง" พร้อม tooltip วันที่เต็มแบบ CSS-only `[data-tooltip]`, หน่วง 300ms ไม่ใช่ ~1 วิแบบ native `title=""`)
  - `includes/menu.php` — `getMenuItems()` อ่านเมนูจากตาราง `mblog_menu_items` (คืนเป็น tree รองรับเมนูย่อย) + ฟังก์ชันสำหรับหน้าแอดมิน (`getAllMenuItems()`, `getTopLevelMenuItems()`, `getMenuItemById()`, `countMenuItemChildren()`, `nextMenuSortOrder()`, `createMenuItem()`/`updateMenuItem()`/`deleteMenuItem()`)
  - `menu.php` — หน้าแอดมินจัดการเมนู (`requireCapability('manage_menu')`) มีลิงก์ในเมนูหลักแล้ว
  - `includes/settings.php` — `siteSetting($key)`/`updateSiteSettings()` อ่าน/เขียนค่าตั้งค่าเว็บจากตาราง `mblog_settings`
  - `settings.php` — หน้าแอดมินตั้งค่าเว็บ (`requireCapability('manage_settings')`) — `require config.php` ตรงๆ ไม่ผ่าน `includes/articles.php` เหมือนหน้าอื่น เพราะไม่ใช้ฟังก์ชันบทความเลยสักตัว — ตอนนี้มีเพิ่ม: **โลโก้เว็บ + favicon** (อัปโหลด+พรีวิว+ลบ, เก็บที่ `uploads/site/logo.<ext>`/`favicon.<ext>` ไฟล์เดียวต่อช่อง อัปโหลดใหม่ลบของเก่าอัตโนมัติแม้เปลี่ยนนามสกุล — โลโก้โชว์ก่อนชื่อเว็บใน header จำกัดสูง 55px กว้าง 80px เสมอด้วย CSS, favicon ออก `<link rel="icon">` จริงพร้อมเดา mime จากนามสกุล), **สโลแกนเว็บ** (ช่องข้อความ + toggle เปิด/ปิด แยกจากกัน — ปิดไว้ก็ยังพิมพ์เก็บไว้ได้ไม่ต้องพิมพ์ใหม่), **ตำแหน่ง sidebar** (ซ้าย/ขวา — ดูหัวข้อ "ระบบ Sidebar item" ด้านล่าง), **token นำเข้า Markdown** (`markdown_import_token` — ดู [PLANNING.md](PLANNING.md) หัวข้อ 7)
  - `categories.php` — หน้าแอดมินจัดการหมวดหมู่ (เพิ่ม/แก้/ลบ, เลือกสีป้ายจาก 7 โทนเดียวกับ `.category-tag-*`) ลบหมวดหมู่ไม่ทำลายบทความ (FK `category_id` เป็น `ON DELETE SET NULL` — บทความแค่กลายเป็น "ไม่มีหมวดหมู่" จริงๆ ไม่ใช่ตกไปหมวดแรกแบบเงียบๆ เหมือนที่เคยเป็น) แต่หน้านี้ก็เตือนจำนวนบทความที่ใช้อยู่ก่อนลบเผื่อกดผิด
  - `admin.php` — dashboard รวมลิงก์ไปหน้าแอดมินทั้งหมด แบ่ง 2 โซน "เนื้อหา" (เขียนบทความ/จัดการบทความ/ร่าง/บทความทั้งหมด/หน้า/นำเข้า Markdown) กับ "ตั้งค่าเว็บ" (ตั้งค่าเว็บ/จัดการเมนู/จัดการหมวดหมู่/จัดการ Sidebar) แต่ละการ์ดโชว์จำนวนคร่าวๆ ดึงจากฟังก์ชันที่มีอยู่แล้ว ไม่ query เพิ่ม — เมนูหลักตอนนี้ยุบ "ตั้งค่า"/"จัดการเมนู" (ที่เคยแยกกัน) เหลือลิงก์เดียวคือ "จัดการเว็บ" ชี้มาที่นี่ กันเมนูบนบวมทุกครั้งที่เพิ่มหน้าแอดมินใหม่ในอนาคต (หน้าแอดมินใหม่ๆ ไม่ต้องมีลิงก์เมนูของตัวเองอีกแล้ว เข้าผ่าน dashboard แทน)
  - **ระบบนำเข้า Markdown** — `import-markdown.php` (อัปโหลดมือ) + `api/import-markdown.php` (รับอัตโนมัติด้วย token) + `includes/markdown-import.php` (แกนแปลง/บันทึกร่วม) — รายละเอียดเต็มดู [PLANNING.md](PLANNING.md) หัวข้อ 6/7 และ Phase 5/6
  - **ระบบ Sidebar item (ทำแล้ว)** — แบนเนอร์/ประกาศ/ป้ายโฆษณา/ป้ายลิงก์ที่โชว์ข้าง `articles.php`/`article.php`/`pages.php`/`page.php`/`category.php`/`tag.php` (หน้าแอดมินไม่มี) ตารางแยกต่างหาก `mblog_sidebar_items` โดยตั้งใจ ไม่ยุบรวมกับ `mblog_articles` แบบ Page เพราะ sidebar item ไม่ใช่หน้าที่มี URL ของตัวเอง (ไม่ต้องมี slug/SEO/tag/category/redirect) — สคีมาเบากว่ามาก: title/content/image/link_url/is_active/sort_order — ลบเป็น hard delete ตรงๆ ไม่มีถังขยะแบบบทความ (ของชิ้นเล็ก ความเสียหายจากลบพลาดต่ำ พอมี popup ยืนยันก็เพียงพอ)
    - `includes/sidebar.php` — data layer (`getActiveSidebarItems()`/`getAllSidebarItems()`/`getSidebarItemById()`/`createSidebarItem()`/`updateSidebarItem()`/`updateSidebarItemOrder()`/`deleteSidebarItem()`/`nextSidebarSortOrder()`)
    - `sidebar-items.php` — หน้ารวม จัดลำดับ+เปิดปิดผ่านฟอร์มเดียว (ช่อง input นอกฟอร์มผูกกลับด้วย `form="reorder-form"` แบบเดียวกับ `manage-articles.php`)
    - `sidebar-item-editor.php` — หน้าสร้าง/แก้ไข ใช้ Quill editor เต็มรูปแบบเหมือน `editor.php` แต่ตัดหมวดหมู่/แท็ก/slug/SEO ออกหมด (เหลือแค่ title/content/image/link_url) — มี image picker ครบทั้งโหมดอัปโหลดไฟล์และ "ใส่ URL แทน" เหมือน `editor.php` ทุกอย่าง — ตัวแปรหลักของ item ที่กำลังแก้ตั้งชื่อ `$sidebarItem` (ไม่ใช้ `$item` เพราะ `partials/header.php` ใช้ `$item` เป็นตัวแปร loop ของเมนูอยู่แล้ว และ `include` แชร์ scope กัน)
    - `api/save-sidebar-item.php` — endpoint บันทึก (คู่กับ `saveSidebarItem()` ใน `assets/editor.js` ที่ใช้ pipeline cleanup เนื้อหาเดียวกับ `saveArticle()`) — normalize เนื้อหาว่างของ Quill (`<p><br></p>`) เป็น `NULL` ก่อนเซฟเสมอ (เช็คว่าไม่มีทั้งตัวหนังสือและรูปฝังในเนื้อหาก่อน) กัน `partials/footer.php` เรนเดอร์กล่องเนื้อหาว่างเปล่าใต้รูป
    - `partials/header.php`/`footer.php` — เปิด/ปิด layout 2 คอลัมน์ (`.main-content` + `<aside class="sidebar">`) เฉพาะหน้าที่ตั้ง `$showSidebar = true` **และ** มีรายการ active จริงอย่างน้อย 1 อัน ไม่งั้น fallback เป็นคอลัมน์เดียวเหมือนเดิมทุกอย่าง — ตำแหน่งซ้าย/ขวาคุมด้วย CSS `order` (ไม่ใช่สลับ DOM) กันเนื้อหาหลักไม่ได้มาก่อนเสมอสำหรับ screen reader, มือถือ stack เนื้อหาหลักไว้บนสุดเสมอไม่ว่าตั้งค่าซ้าย/ขวา
    - **เปิด/ปิด sidebar แบบ 2 ชั้น (ทำแล้ว)** — ชั้นเว็บ: `sidebar_enabled` ใน `mblog_settings` (checkbox ใน `settings.php`) ชั้นบทความ/หน้า: คอลัมน์ `mblog_articles.show_sidebar` (`TINYINT(1) NULL`, `database/article_sidebar_toggle.sql`) เลือกได้ใน `editor.php` เป็น dropdown 3 ทาง "ตามค่าเว็บ/เปิด/ปิด" — `NULL` = ตามค่าเว็บ **แบบ live** (ไม่ snapshot ค่าตอนสร้าง เปลี่ยน setting เว็บทีหลังก็ขยับตามทันที), `1`/`0` = บังคับเปิด/ปิดสำหรับบทความ/หน้านั้นโดยเฉพาะ **ไม่สนใจค่าเว็บเลย** (priority สูงสุด) — สรุป logic อยู่ที่ `articleShowsSidebar()` ใน `includes/articles.php` เรียกจาก `article.php`/`page.php` เท่านั้น (หน้ารายการทั้ง 5 ไม่มีแนวคิด "รายการเดียว" ให้ผูกค่าต่อชิ้น เลยยึด `sidebar_enabled` ของเว็บตรงๆ ต่อไปผ่าน `partials/header.php`) — `partials/header.php` มีตัวแปร `$sidebarSiteGate` (default true) ให้ `article.php`/`page.php` ปิดการเช็ค `sidebar_enabled` ซ้ำ เพราะ resolve ค่าสุดท้ายมาให้แล้วจาก `articleShowsSidebar()`
    - **ความกว้าง header/footer คงที่ทุกหน้า (ทำแล้ว)** — topbar/footer ใช้ class `container-wide` (1100px) ทุกหน้าเสมอไม่มีเงื่อนไข (รวมหน้าแอดมิน) — ส่วนเนื้อหา: มี sidebar ใช้ 800(เนื้อหา)+20(gap)+280(sidebar)=1100 ตรงกับ header เป๊ะ, ไม่มี sidebar ใช้ 960
    - **ลิงก์ sidebar เปิด tab ใหม่ (ทำแล้ว)** — `target="_blank" rel="noopener noreferrer"` ใน `partials/footer.php`
    - **CSS ของเนื้อหาที่มาจาก Quill ใช้ร่วมกับบทความ (ทำแล้ว)** — รวมอยู่ใต้ class กลาง `.rich-content` ใน `assets/layout.css` (โหลดทุกหน้า) ครอบคลุมพฤติกรรมของ Quill ทั้งหมด (จัดกึ่งกลาง/รูป float/caption/blockquote/code block/hr/bullet-numbered list marker) ส่วนขนาด/ระยะห่างที่ต่างกันตามบริบท (คอลัมน์บทความ 800px vs การ์ด sidebar 280px) แยกกำหนดเฉพาะจุด — `quill.snow.css` (vendor) โหลดครั้งเดียวใน `partials/header.php` ทุกหน้า, wrapper ที่ render เนื้อหาแบบอ่านอย่างเดียวมี class `rich-content ql-editor` คู่กันเสมอ — padding ของโหมดแก้ไขจริง (`.ql-editor { padding: 12px 15px }`) ถูก cancel เฉพาะบริบทบทความเต็มคอลัมน์เท่านั้น (`.article-content.ql-editor { padding: 0 }`) ไม่กระทบ padding 14px ของการ์ด sidebar
    - **วิดีโอฝัง (`.ql-video`) ใน sidebar item (ทำแล้ว)** — ไม่มี margin ส่วนเกินของตัวเอง (`.sidebar-item-content .ql-video { margin: 0; }`) อยู่ในกรอบ padding 14px ของการ์ดเดียวกับเนื้อหาอื่น (ไม่ full-bleed ชิดขอบแบบรูปภาพ)
  - **`manage-articles.php`** — หน้าจัดการบทความแบบ WP (checkbox เลือกหลายแถว + bulk action, ค้นหา/กรองด้วยข้อความ/หมวดหมู่/แท็ก/ช่วงวันที่, แท็บสถานะทั้งหมด/เผยแพร่แล้ว/ร่าง/ถังขยะ พร้อม pagination) — แยกจาก `articles.php` เดิมโดยเจตนา (ตัวนั้นยังเป็นฟีดสาธารณะ เผยแพร่แล้วเท่านั้น) ขอบเขตแค่ `type=post` เท่านั้น ไม่รวม page
    - **ระบบถังขยะ (ทำแล้ว)** — เพิ่มคอลัมน์ `deleted_at` แยกจาก `status` โดยตั้งใจ (ไม่ใช้ค่า `trash` ใน `status` ENUM) เพราะกู้คืนต้องรู้ว่าของเดิมเป็นร่างหรือเผยแพร่แล้วกันแน่ — ลบ = set `deleted_at`, กู้คืน = เคลียร์เป็น NULL, ลบถาวรถึงจะ `DELETE` จริง (DB cascade ลบ tag/image/redirect ให้เอง ไม่ลบไฟล์รูปจริงตามหลักการเดิมของ `syncArticleImages()`)
    - `fetchArticles()`/`fetchOneArticle()` (จุดรวมที่ทุกฟังก์ชันอ่านบทความเรียกผ่าน) ผูก `deleted_at IS NULL` ไว้ในตัวเองแล้ว กันบทความที่อยู่ถังขยะหลุดออกไปทุกหน้าโดยอัตโนมัติ ไม่ต้องแก้ทีละฟังก์ชัน — `getArticlesForAdmin()` เป็นข้อยกเว้นเดียวที่ตั้งใจข้าม helper นี้เพราะต้องมองเห็นถังขยะได้
    - ปุ่มลบทุกจุด (แถวเดี่ยว + bulk) มี popup ยืนยันก่อนเสมอ (`confirm()`) โดยเฉพาะ "ลบถาวร" ที่เตือนชัดว่ากู้คืนไม่ได้
    - **บั๊กที่เจอระหว่างทำและแก้แล้ว**: ฟอร์ม bulk action ครอบทั้งตาราง แล้วแต่ละแถวก็มีฟอร์มลบของตัวเองซ้อนอยู่ข้างใน — `<form>` ซ้อน `<form>` เป็น HTML ที่ไม่ถูกต้อง เบราว์เซอร์จะรวมฟอร์มในเข้ากับฟอร์มนอกให้เงียบๆ (กดปุ่มลบแถวเดียวกลับไปเรียก handler ของ bulk form แทน) แก้ด้วยการย้าย checkbox ออกมานอกฟอร์ม แล้วผูกกลับด้วย attribute `form="bulk-form"` แทนการครอบด้วย `<form>` จริง
  - `search.php` + `assets/search.js` — ค้นหาสาธารณะ (ดู [PLANNING.md](PLANNING.md) หัวข้อ 2), ไอคอนแว่นขยายในแถบบนสุด
  - `includes/db.php` — จุดเชื่อมต่อ MySQL (PDO) เดียวของทั้งแอป ทุกฟังก์ชันข้างบนเรียกผ่าน `db()`
  - `config.php` (gitignored) / `config.example.php` — path, `APP_ENV`, DB credentials (ต้อง define ก่อน require `includes/settings.php` เพราะ `siteSetting()` ต้องใช้ `db()` แล้ว), บังคับ timezone ตายตัวจาก `mblog_settings` (กัน php.ini ของแต่ละเครื่อง/แต่ละ PHP ที่รันไม่ตรงกัน)
  - `database/*.sql` — SQL migration แยกไฟล์ตาม Phase (`phase1_core.sql` ถึง `phase9_stats.sql`) ทุกตารางขึ้นต้นด้วย `mblog_` กันชนกับระบบอื่นถ้าต้องใช้ฐานข้อมูลร่วมกันบนโฮสที่มีได้แค่ DB เดียว
  - `uploads/YYYY/MM/` — เก็บรูปภาพที่แทรก แยกโฟลเดอร์ตามเดือนกันไฟล์บวมในโฟลเดอร์เดียว
  - `assets/base.css`, `layout.css`, `components.css`, `article.css`, `editor.css`, `stats.css` — แยกจาก `style.css` ไฟล์เดียวเดิม (`editor.css` โหลดเฉพาะหน้าแก้ไข, `stats.css` โหลดเฉพาะ `stats.php` — ไม่ส่งให้ผู้อ่านทั่วไป), `assets/editor.js`, `assets/copy-button.js`, `assets/menu.js` — เมนูย่อย desktop เปิดด้วย CSS `:hover`/`:focus-within` ล้วนๆ (ไม่ใช้ JS toggle อีกต่อไป เพราะตัวคำหลักกลายเป็นลิงก์จริงแล้ว), ส่วน mobile accordion ยังคลิกเปิด/ปิดผ่าน JS เหมือนเดิม (คนละ component กันกับ desktop โดยสิ้นเชิง)
  - `assets/toast.js` — โหลดทุกหน้า (เหมือน `menu.js`/`theme.js`) ลบ query param แบบ one-time (`?saved=1`/`?deleted=1`/`?done=1` — ใช้แพทเทิร์นเดียวกับที่ `manage_price_alerts.php` ของโปรเจกต์ z/aset ทำ) ออกจาก address bar ด้วย `history.replaceState` หลังโชว์ toast ครั้งเดียว กันรีเฟรชแล้วโชว์ toast ซ้ำ/ค้าง query string
  - **Toast notification (ทำแล้ว)** — เปลี่ยน `.settings-notice` (ใช้ร่วมกันใน `settings.php`/`menu.php`/`categories.php`/`manage-articles.php` อยู่แล้ว) จากกล่องแจ้งเตือนที่นั่งอยู่ในเนื้อหาหน้า เป็น toast ลอย (`position:fixed` กึ่งกลางบนจอ) พร้อม CSS keyframe เลื่อน+จางเข้า — **success จางหายเองใน 3.2 วิ, error ไม่หายเอง** (ต้องอ่าน/แก้ก่อน) — แก้จุดเดียวใน `components.css` มีผลครบ 4 หน้าที่ใช้ class นี้ทันที ไม่ต้องแก้ทีละหน้า
  - **ธีมเว็บ (ทำแล้ว)** — restyle ตาม `claude-design.md` (cream canvas + coral accent + dark-navy surface) พร้อมโหมด light/dark สลับได้ `assets/theme.js` — สีทั้งหมดอยู่ใน `assets/base.css` เป็น 2 ชั้น:
    1. **Raw palette** (`--color-*`) — เกือบทั้งหมด theme-aware: มีค่า light ใน `:root` และคู่ dark ชื่อเดียวกันบวก suffix `-dark` (เช่น `--color-canvas` ↔ `--color-canvas-dark`) รวมถึง `--color-primary`/`--color-on-primary` ด้วย (ปุ่มหลักธีมมืดใช้เฉดสว่างขึ้น + ตัวหนังสือเข้มแทนขาว เพราะขาวบนเฉดสว่างคอนทราสต์ไม่พอ) — footer/code block ดึงตัว `-dark` มาใช้ตรงๆ ไม่ผ่าน override เพราะไม่สลับตามธีม — สีระบบที่ไม่ผูกกับแบรนด์ (accent teal/amber, สถานะ, tag 7 โทน, `--dropdown-shadow`) แยกโซนไว้ท้าย `:root` ไม่ถือเป็น org-customizable
    2. **Role tokens** — สิ่งที่ component เรียกใช้จริง (`--page-bg`, `--card-bg` ฯลฯ) map ไปยัง raw palette ครั้งเดียว รับค่าใหม่อัตโนมัติผ่าน cascade ตอนสลับธีม — ไม่มี `--color-primary-hover`/`--btn-secondary-bg-hover` แบบ token เฉพาะอีกต่อไป ปุ่ม/ลิงก์ที่ hover ต้อง "เข้มขึ้น" ใช้ `filter: brightness(0.8)` คำนวณจากสีปกติแทน (ไม่ต้องเพิ่ม token ใหม่, ปรับสีเดียวจบทั้ง 2 state)
    - **`color-reference.php`** (หน้าแอดมิน) — โชว์เฉพาะสี org-customizable (light + dark ของ raw palette theme-aware) พร้อมตาราง role token ว่าใช้ตรงไหนบ้าง — สีระบบไม่แสดงในหน้านี้โดยตั้งใจ
    - **`theme-preview.php`** (หน้าแอดมิน, คู่กับข้างบน) — เพจตัวอย่าง render component จริง (เมนู, ปุ่ม, ฟอร์ม, badge, ลิงก์) ด้วยสีปัจจุบัน มีคู่ปกติ/hover วางเทียบกันให้เห็นทันทีไม่ต้องเอาเมาส์ไปวางจริง ไว้ตรวจความสวยงามเวลาปรับสีในไฟล์ข้างบน
    - **`theme-editor.php` + `includes/theme-colors.php` (ทำแล้ว) — ปรับแต่ง/export/import ชุดสีได้จริงจากหน้าแอดมิน** โดยไม่แตะ `assets/base.css` เลย: ค่า override เก็บเป็น JSON blob เดียวใน `mblog_settings` (คีย์ `custom_theme_colors`, validate ทั้ง whitelist ชื่อ token และ regex hex ก่อนบันทึก/ก่อน import เสมอ เพราะค่าจะถูก echo เป็น `<style>` ตรงๆ ทุกหน้า) `partials/header.php` echo override ทับหลัง `components.css` เท่านั้น (ไม่มี override = ไม่มี `<style>` เพิ่มเลย) — ค่าเริ่มต้นจริง (`defaultThemeColors()`) เป็นคนละที่เก็บจาก override เสมอ กด "คืนค่าเริ่มต้น" คือลบ override ทิ้งอย่างเดียว ไม่มีทางเขียนทับค่าเริ่มต้นได้จากการใช้งานปกติ — โหมดง่าย (สีหลัก/โทนพื้นหลัง 4 แบบ/สีลิงก์ คำนวณที่เหลือให้อัตโนมัติด้วยสูตร HSL ฝั่ง JS) กับโหมดขั้นสูง (22 ช่องสีจริงตรงกับ `color-reference.php`) สลับไปมาได้ ค่าที่คำนวณจากโหมดง่ายเติมให้โหมดขั้นสูงอัตโนมัติ — พรีวิวสดในหน้าเดียวกัน (ไม่ต้อง save ก่อน) ครอบคลุมทุก state ที่เคยมีปัญหา hover มองไม่เห็นในหัวข้อถัดไปด้วย (เมนู, dropdown, sidebar ปกติ/hover/active, ปุ่มหลัก/รอง ปกติ/hover) — ทุกช่องสีมีคู่สวอตช์+กล่องพิมพ์ hex ตรงๆ (พิมพ์ค่าไม่ครบ 6 หลักจะยังไม่อัปเดตจนกว่าจะครบ กัน state ค้างพัง)
    - **Hover ที่ทับสีพื้นหลังตัวเองพอดี (บั๊กที่เจอและแก้แล้ว)** — จุดที่ hover-background เดิมชี้ไป `--color-card` ตัวเดียวกับพื้นหลัง panel ที่มันวางอยู่ (dropdown submenu, `.dashboard-card`, tag suggestion ใน editor) ทำให้ hover มองไม่เห็นเลย แก้โดยเปลี่ยนไปใช้ `--card-border` แทน (มีอยู่แล้ว คอนทราสต์ต่างชัดเจนทั้ง 2 ธีม)
    - **`.btn-secondary:hover` โดน `filter` จาก `.btn:hover` ทับซ้อนโดยไม่ตั้งใจ (บั๊กที่เจอและแก้แล้ว)** — element เดียวมีทั้ง class `btn`+`btn-secondary` เลยโดนทั้ง 2 กฎ (คนละ property เลยไม่ override กัน แต่บวกกัน): `.btn-secondary:hover` เปลี่ยน background เป็น `--color-card` (ครีมอ่อน) แล้ว `.btn:hover { filter: brightness(0.8) }` มาหรี่ทับอีกที กลายเป็นสีเทาหม่นแทนที่จะเป็นครีมสะอาด — แก้ด้วย `filter: none` ใน `.btn-secondary:hover`
    - **`appearance: none` ให้ `.btn` (บั๊กที่เจอและแก้แล้ว)** — ไม่เคยตั้งมาก่อนทั้งเว็บ ทำให้ Safari ใส่เงาปุ่มระบบสีเทาทับปุ่มทุกปุ่มตอน hover/active เอง โดยไม่สนสีพื้นหลังที่ CSS กำหนดไว้ (Chrome/Firefox ไม่ทำแบบนี้ เลยไม่เคยสังเกตเห็น)
    - **สีลิงก์ (`--color-link`) เป็น global ผ่าน `a { color: var(--link-color); }`** ใน `base.css` ครอบคลุมลิงก์เปล่าทุกหน้า ไม่ใช่แค่ในเนื้อหาบทความเหมือนตอนแรก (selector ที่มี class อยู่แล้วเช่นปุ่ม/เมนู/badge specificity สูงกว่า ไม่กระทบ) — **hover ของลิงก์ตั้งใจไม่ทำ global** (เคยลองแล้วไปทับ hover เดิมของ sidebar/การ์ดที่ไม่ได้ตั้ง filter เอง ทำให้มืดเกินตั้งใจ) จำกัดไว้แค่ `.rich-content a:hover`/`.ql-editor a:hover` (ลิงก์ในเนื้อหาบทความ) เท่านั้น
    - Footer + code block เป็น dark navy/dark window ตายตัวทั้ง 2 ธีม ("footer never inverts") ไม่มีคู่ light เลย พร้อมสลับ highlight.js เป็น `github-dark.min.css` คู่กันกันสีโค้ดอ่านไม่ออก
    - Quill editor canvas (`#editor-container`) จงใจไม่ตามธีม เพราะ Quill บังคับสีตัวหนังสือมาจาก snow.css ของมันเอง
    - ฟอนต์ `"Segoe UI", "Sarabun", Tahoma, sans-serif` ตัวเดียวทั้งเว็บ
    - Toggle ธีม: ปุ่มใน topbar ทุกหน้า จำค่าที่ `localStorage` — inline script เดียวใน `partials/header.php` เช็คทั้ง `localStorage` และ `prefers-color-scheme` เซ็ต `data-theme` ก่อน paint เลย ไม่มี `@media` ซ้ำฝั่ง CSS
    - **Header แยกเป็น 2 แถว (ทำแล้ว)** — `.topbar-brand-row` (โลโก้+ชื่อเว็บ ตัวใหญ่ขึ้น 27px + ปุ่ม action) กับ `.topbar-nav-row` (เมนู, พื้นหลังสีส้ม coral `--color-primary` เด่นชัดแยกจากพื้นเว็บ) — selector สีลิงก์ใช้ `.topbar-nav-row .topbar-menu > a` (child selector เจาะจงแค่ลิงก์ชั้นบนสุด กันไปทับสีลิงก์ใน dropdown ย่อยที่พื้นหลังอ่อนกว่า)

  - **ระบบสถิติ (Phase 9, ทำเสร็จ 2026-07-27 — ดู [PLANNING.md](PLANNING.md) หัวข้อ 5/Phase 9 สำหรับสถานะ/งานค้าง)** — เก็บ pageview ทั้งเว็บ (ไม่ใช่แค่บทความ) แบบ server-side ล้วนๆ ไม่ต้องมี tag ฝัง
    - `database/phase9_stats.sql` — `mblog_article_stats` (`article_id` PK/FK, `view_count`, ยอดรวมตลอดกาลต่อบทความ อ่านเร็วไม่ต้อง `COUNT()`) + `mblog_pageview_log` (log ราย pageview ทั้งเว็บ ไม่ใช่แค่บทความ: `article_id` **nullable**, `page_type`/`page_path` รองรับหน้าประเภทใหม่ในอนาคตโดยไม่ต้องแก้ schema, `visitor_hash`, `user_agent`, `device_type`, `os`, `is_bot`, `referrer`, `viewed_at`)
    - `includes/stats.php` — `recordPageview(string $pageType, ?int $articleId = null)` เรียกจาก 6 หน้า (`article.php` ส่ง `'article'`+id เท่านั้นที่ bump `view_count` ด้วย, `articles.php`/`category.php`/`tag.php`/`page.php`/`search.php` ส่งแค่ type ของตัวเอง `article_id` เป็น null) — ไม่แตะหน้าแอดมิน/editor/`index.php` (redirect ไป `articles.php` อยู่แล้ว นับซ้ำถ้านับด้วย) เพราะไม่ใช่ traffic ผู้อ่านจริง
      - **ไม่เก็บ IP ดิบเลย** — `computeVisitorHash()` ใช้ `hash_hmac('sha256', $ip, STATS_HASH_SECRET . date('Y-m-d'))` (secret ใหม่ใน `config.php`/`config.example.php`) นับ unique visitor ต่อวันได้โดยไม่แบกความเสี่ยง PDPA — hash หมุนทุกวัน แปลว่า track ข้ามวันไม่ได้ (ตั้งใจ)
      - `parseDeviceType()`/`parseOs()` — regex ธรรมดาเทียบ `user_agent` เอง ไม่พึ่งไลบรารีนอก (ตัดสินใจไม่ใช้ MaxMind GeoLite2 เพราะต้องพึ่ง Composer ที่โปรเจกต์นี้ไม่เคยใช้ — เรื่อง geo/ประเทศตัดออกจากขอบเขตทั้งหมด ให้ Google Analytics ทำแทน) — ลำดับเช็คสำคัญ: iPhone → iPad → Android (ดู `Mobile` token แยกโทรศัพท์/แท็บเล็ต) → Windows/Mac/Linux → other (Android ต้องเช็คก่อน Linux เพราะ UA ของ Android มีคำว่า Linux ปนอยู่ด้วย ไม่งั้น Android phone จะถูกนับเป็น Linux desktop ผิด) — ข้อจำกัดที่ยอมรับแล้ว: iPad ตั้งแต่ iPadOS 13+ (2019) ปลอมตัวเป็น Macintosh ในเบราว์เซอร์เริ่มต้น ทำให้ iPad ส่วนใหญ่ถูกนับเป็น `desktop`/`macos` แทน (ไม่ทำ JS beacon แก้เพราะ ROI ต่ำ)
      - `isBotUserAgent()` — เทียบ pattern บอทที่รู้จัก (`bot`/`spider`/`crawl`/`curl`/`python-requests`/...) + UA ว่างถือเป็นบอท — log ไว้เหมือนเดิม (เหมือน GA's bot filtering) แค่ไม่บวก `view_count`/นับ unique visitor — **ต้องทบทวน/อัปเดต pattern เป็นระยะ** เพราะบอทใหม่ๆ โผล่มาเรื่อยๆ (ยังไม่ได้ทำ ไม่มีกำหนด)
      - **บั๊กที่เจอและแก้แล้ว**: `recordPageview()`/`articleViewCount()` เดิมไม่มี try/catch — `db()` ตั้ง `PDO::ERRMODE_EXCEPTION` ไว้ ถ้า insert/update สถิติพลาด (lock timeout, ดิสก์เต็ม) exception จะหลุดไปโดน `set_exception_handler` ใน `includes/error-handling.php` กลายเป็นหน้า 500 ทั้งที่บทความโหลดสำเร็จแล้ว — ครอบ try/catch แล้ว log error เฉยๆ ไม่ทำให้หน้าเว็บพัง (ทดสอบจริงด้วยการ rename ตาราง `mblog_pageview_log` ออกชั่วคราวแล้วยิง request เข้าไป ได้ HTTP 200 ตามที่ตั้งใจ) — สังเกตเพิ่มระหว่างทดสอบ: `error_log()` ที่ตั้งใจให้ลง `logs/php-error.log` จริงๆ ไปลงที่ log กลางของ Apache/XAMPP แทนเวลารันผ่านเว็บ (พฤติกรรมเดิมของทั้งโปรเจกต์ ไม่ใช่บั๊กใหม่)
    - `stats.php` — หน้า dashboard ดูสถิติ (CSS bar chart ล้วน ไม่พึ่ง Chart.js) มี 4 ช่วงเวลา (`?range=today|7d|30d|all`) — `statsDateRange()` ใช้ `-6`/`-29` วัน ไม่ใช่ `-7`/`-30` (บั๊กที่เจอแล้วแก้: นับรวมวันนี้ด้วยแล้วยังใช้ `-7` วันจะได้ 8 วันจริง ไม่ใช่ 7) — โชว์การ์ดสรุป (pageview/unique visitor/บอท), กราฟแท่งรายวัน (เติมวันที่ไม่มีข้อมูลเป็น 0 กันแกนขาดช่วง เฉพาะช่วงเวลาที่มีขอบเขตชัด ไม่ทำกับ `all`), ตารางบทความยอดนิยม**ในช่วงเวลาที่เลือก** (คนละตัวเลขกับ `mblog_article_stats.view_count` ที่เป็นยอดรวมตลอดกาล), แยกอุปกรณ์/OS/ประเภทหน้า/ที่มา — `statsTopReferrers()` group by โดเมน (ไม่ใช่ URL เต็ม เพราะ URL เต็มแทบไม่ซ้ำกันเลย จะกลุ่มไม่ได้ความหมาย) และตัด referrer ที่มาจากโดเมนตัวเองออก (การนำทางภายในเว็บ ไม่ใช่ traffic source) — query ตรงจาก `mblog_pageview_log` เอง ยังไม่มี rollup table (ตัดสินใจพักไว้ก่อนเพราะข้อมูลยังน้อยเกินจะเห็นภาพจริงว่าควรสรุประดับไหน)
    - ช่องใส่โค้ด `<head>`/`</body>` ใน `settings.php` (`custom_head_code`/`custom_body_code`, render ผ่าน `siteSetting()` แบบ raw ไม่ escape โดยตั้งใจ ใน `partials/header.php`/`footer.php`) — ไม่ใช่ส่วนหนึ่งของระบบสถิติเอง (ระบบเราเป็น server-side ไม่ต้องมี tag) แต่เป็นช่องทางสำหรับ tag ของนอกที่ต้องพึ่ง client-side script (Google Analytics/GTM/Facebook Pixel ในอนาคต) — เป็น arbitrary script injection ทั้งเว็บโดยเจตนา ความเสี่ยงที่เคยกังวลไว้ (ตอนนั้น `settings.php` ยังไม่มีระบบล็อกอินป้องกัน) ปิดไปแล้วหลัง Phase 3 (`requireCapability('manage_settings')`)
    - **พักไว้ ยังไม่ทำ**: retention/purge ของ `mblog_pageview_log` (แผนที่คุยไว้: cron รายคืนสรุปยอดเมื่อวานเข้า rollup table ก่อน แล้วลบ raw log ที่เก่ากว่า 90 วัน — มีช่วงกันชน 89 วันกัน cron พลาดข้อมูลหาย, ยังไม่ตัดสินใจระดับความละเอียดของ rollup), เก็บระยะเวลาดูหน้า (time on page — ต้องมี client-side JS beacon เป็นของตัวเองเป็นครั้งแรก วัดได้ไม่แม่นยำโดยธรรมชาติ ตัดสินใจไม่ทำแล้วใช้ Google Analytics ผ่านช่องใส่ tag แทนถ้าต้องการจริง), เก็บ geo/ประเทศ (ตัดออกจากขอบเขตทั้งหมด ให้ GA ทำแทน)

  - **ระบบแอดมิน sidebar (เกิดขึ้นระหว่างทำ `stats.php` แล้วขยายเป็นงานแยก 2026-07-27 — ดู [PLANNING.md](PLANNING.md) หัวข้อ "หลักการรอง" สำหรับหลักการที่สรุปจากงานนี้)** — sidebar เมนูถาวรทุกหน้าแอดมิน แทนที่การกดย้อนกลับไป `admin.php` ทีละหน้า
    - `includes/admin-nav.php` — `adminNavGroups()` (ข้อมูลเมนู sidebar 3 กลุ่ม: เนื้อหา/สถิติ/ตั้งค่าเว็บ พร้อม badge จำนวนจาก `getArticles()`/`getDraftArticles()`/`getAllMenuItems()`/`getAllCategories()`/`getAllSidebarItems()`) — จุดเดียวที่รู้รายการเมนูทั้งหมด ต่อยอด pattern เดียวกับ `includes/manage-list.php`'s `renderManageListPage()` ("one engine, thin callers") ที่มีอยู่แล้ว — ลิงก์ "← จัดการเว็บ" ที่ topbar เดิมเคยมาจากฟังก์ชัน `adminTopbarActions()` ในไฟล์นี้ ย้ายออกไปแล้วหลัง Phase 15 (ดูหัวข้อ "ระบบล็อกอิน/สิทธิ์" ด้านล่าง) — ตอนนี้อยู่บนสุดของ sidebar แทน
    - `partials/admin-sidebar.php` — render sidebar จาก `adminNavGroups()` พร้อม active-state highlight (เทียบ `basename($_SERVER['SCRIPT_NAME'])` กับ href ของแต่ละรายการ) — root element ใช้ **`class="sidebar admin-sidebar-shell"`** ยืม engine เดิมของ sidebar เนื้อหาสาธารณะ (`.sidebar`/`.container-with-sidebar`/`.sidebar-left` ใน `assets/layout.css`) ตรงๆ ไม่สร้าง CSS คู่ขนานใหม่ — ต่างจาก sidebar สาธารณะแค่ 2 จุด: (1) บังคับตำแหน่งซ้ายเสมอผ่าน `$sidebarPosition = $showAdminSidebar ? 'left' : siteSetting('sidebar_position', 'right');` ใน `partials/header.php` ไม่ขึ้นกับค่าตั้งเว็บ (2) ความกว้าง sidebar 240px แทน 280px ผ่าน CSS variable `--sidebar-width` (`.sidebar { width: var(--sidebar-width, 280px); }` + `.admin-sidebar-shell { --sidebar-width: 240px; }` — public sidebar ไม่ถูกกระทบเพราะไม่มีใคร override ตัวแปรของมัน) — `partials/header.php`/`footer.php` มีตัวแปร `$showAdminSidebar` แยกจาก `$showSidebar`/`$hasSidebar` เดิมโดยสิ้นเชิง (คนละ feature กัน แค่ใช้กลไก layout เดียวกัน)
    - ใช้ใน 11 หน้าแอดมิน: `admin.php`, `stats.php`, `manage-articles.php`/`manage-pages.php` (ผ่าน `includes/manage-list.php` จุดเดียว ครอบทั้งคู่), `menu.php`, `categories.php`, `sidebar-items.php`, `settings.php`, `import-markdown.php`, `drafts.php`, `editor.php`, `sidebar-item-editor.php` — **ตั้งใจไม่ใส่**ในหน้าสาธารณะ (`articles.php`/`category.php`/`tag.php`/`page.php`/`search.php`/`article.php`) เพราะจะเผยโครงสร้างแอดมินให้ผู้อ่านทั่วไปเห็น
    - **บั๊กที่เจอและแก้แล้ว**: hover เมนูที่ active อยู่ใน light theme อ่านตัวหนังสือไม่ออก — `.admin-sidebar-link:hover` (specificity 1 คลาส + 1 pseudo-class) ชนะ `.admin-sidebar-link-active` (1 คลาส) สำหรับ property `background` ทำให้ hover ทับพื้นหลังส้มเป็นสีอ่อนแต่ตัวหนังสือยังขาว (มาจาก `.admin-sidebar-link-active` ที่ยังชนะ property `color` เพราะ hover rule ไม่ได้ประกาศ color แข่งด้วย) — แก้ด้วย `.admin-sidebar-link-active:hover` เจาะจงคืนพื้นหลัง/สีตัวหนังสือให้ตรงกันเสมอ (ทดสอบจริงด้วย mouse hover event ผ่านเบราว์เซอร์ ไม่ใช่แค่จำลอง)
    - **Role-aware แล้ว (หลัง Phase 3)** — `partials/admin-sidebar.php` กรองแต่ละรายการผ่าน `userCan($item['capability'])` ก่อน render เสมอ (capability string เดียวกับที่หน้านั้นเช็คเองผ่าน `requireCapability()`) — author/editor ไม่เห็นลิงก์ไปหน้าที่ตัวเองจะโดน 403 ทันทีอยู่แล้ว กลุ่มที่ไม่เหลือรายการเลย (เช่น "ตั้งค่าเว็บ" สำหรับ author) ก็ถูกซ่อนทั้งกลุ่มไปด้วย ไม่โชว์หัวข้อเปล่าๆ
    - **ความกว้าง container ปรับร่วมด้วย**: `.container-with-sidebar`/`.container-wide` จาก 1100px → **1140px** (ผู้ใช้ปรับเอง เหตุผล: หาร 3/6/12 ลงตัว ต่อยอด grid ง่ายกว่า ใกล้เคียง Bootstrap breakpoint "xl" 1140px) + padding ซ้าย-ขวาของ `.container` ฐานจาก 20px → 10px (มีผลเนื้อหาทุกหน้าบน desktop/tablet เท่านั้น — topbar/footer มีกฎ padding เฉพาะของตัวเองที่เจาะจงกว่าเลยไม่ถูกกระทบ, มือถือ ≤600px มี media query แยกอยู่แล้วก็ไม่ถูกกระทบเช่นกัน — ยืนยันด้วยตัวเลขจริงจากเบราว์เซอร์: topbar/footer ยัง `14px 20px`/`24px 20px` เท่าเดิม)

  - **ระบบฟีดข่าวสั้น (นอกแผนเดิม, ทำเสร็จ 2026-07-27)** — เว็บนี้มีเนื้อหา 3 แบบ: (1) บทความเขียนเอง (2) บทความสรุปโดย AI (3) ข่าวสั้นๆ เช่นประกาศกำไรหุ้น — (1)+(2) โครงสร้างเหมือนกันเป๊ะ (title/เนื้อหาเต็ม/SEO/หมวดหมู่) ต่างแค่ใครเขียน ซึ่งแก้ไปแล้วด้วยแท็ก "สรุปโดย AI" ใน `mblog_articles` ตัวเดียวกัน — (3) โครงสร้างต่างสิ้นเชิง (ข้อความสั้น ไม่มี SEO/รูปปก/หมวดหมู่ ความถี่สูงกว่ามาก) เลยแยกตารางใหม่ `mblog_feed_items` (เหตุผลเดียวกับ sidebar item ไม่ยุบรวมกับบทความ)
    - `database/feed_items.sql` — สคีมาเรียบง่ายที่สุดเท่าที่เป็นไปได้: `id`, `content` (TEXT), `created_at` — ไม่มี `updated_at` เพราะดีไซน์ตัด "ต้องแยกรู้ว่าอะไรถูกแก้ไข" ออกไปแล้ว (ดูหัวข้อ live-update ด้านล่าง) — `id` เป็นตัวขับทั้งลำดับแสดงผลและตัว cursor ที่ใช้ poll หาของใหม่ ไม่ใช้ `created_at` ตัดสิน เพราะข้อความพวกนี้เป็นเรียลไทม์ไม่มีเคสย้อนเวลาแบบไฟล์ Markdown นำเข้า
    - `includes/feed.php` — data layer: `getFeedItems($limit)`, `getFeedItemById()`, `createFeedItem()`, `updateFeedItem()` (ไม่แตะ `created_at` ตอนแก้ไข กันข้อความเด้งขึ้นบนสุดทั้งที่แค่แก้คำผิด), `deleteFeedItem()`, `countFeedItems()` (สำหรับ badge จำนวนใน dashboard/sidebar แอดมิน) + `renderFeedItemHtml($item, $lastSeenId)` — จุดเดียวที่ render markup ของ 1 ข้อความ ใช้ร่วมกันทั้ง `feed.php` (โหลดหน้าแรก) และ `api/feed-poll.php` (live update) กันโค้ด escape/format ซ้ำสองที่แล้วเพี้ยนไม่ตรงกัน
    - **เข้าข้อมูลได้ 2 ทาง** (เหมือน pattern ระบบนำเข้า Markdown): `manage-feed.php` (พิมพ์เอง — มีฟอร์มโพสต์ใหม่ + แก้ไข/ลบต่อรายการ) และ `api/import-feed.php` (รับอัตโนมัติจาก pipeline ภายนอก ด้วย token — **คนละ token กับ `markdown_import_token` โดยตั้งใจ** เก็บเป็น `feed_import_token` แยก เพื่อแยกความเสี่ยง token รั่วจุดหนึ่งไม่กระทบอีกจุด) — ฝั่ง API รับ raw text เป็น body ตรงๆ ไม่ต้องมี JSON wrapper เพราะต้นทางส่งมาแค่ข้อความสั้นๆ แบบ tweet
    - **`manage-feed.php`: แก้ไขใช้ช่อง "ข้อความใหม่" ร่วมกับโพสต์** — กดแก้ไขแถวไหน ช่องพิมพ์บนสุด (`$editItem` มีค่า) เปลี่ยน label/ปุ่มเป็นโหมดแก้ไข + มีปุ่ม "ยกเลิก" ตารางด้านล่างเป็นแถวเรียบง่าย ไม่มีฟอร์มแทรกในแถว — เช็ค "แถวไหนกำลังแก้ไข" ด้วย `(int) $item['id'] === $editId` (ต้อง cast `$item['id']` เป็น int ก่อนเทียบ เพราะ PDO คืนค่าคอลัมน์ตัวเลขเป็น string เสมอ)
    - **`feed.php`: หน้าแสดงผลสาธารณะ เปิดค้างไว้แล้วข้อความใหม่ขึ้นเองอัตโนมัติ** — ใช้ **polling** ทุก 20 วินาที (`POLL_INTERVAL_MS`, ไม่ใช่ SSE/WebSocket เพราะ mod_php ปกติไม่เหมาะกับ connection ค้างนาน และฟีดนี้ไม่ได้อัปเดตถี่ระดับวินาที), silent catch ถ้า fetch ล้มเหลว (รอบถัดไปลองใหม่เอง)
      - `api/feed-poll.php` render HTML ของ**ทั้งชุดปัจจุบันสดใหม่ทุกครั้ง**ด้วย `renderFeedItemHtml()` ตัวเดียวกับที่ `feed.php` ใช้ตอนโหลดหน้าแรก แล้วส่ง HTML ก้อนเดียวกลับ — `assets/feed.js` เหลือแค่ `fetch()` + `list.innerHTML = data.html` ไม่มี escape/DOM-building logic ฝั่ง JS เลย — ข้อดี: แก้ไข/ลบข้อความ sync ไปหน้าที่เปิดค้างไว้ได้ทันทีโดยอัตโนมัติ เพราะทุก poll คือ "ความจริงปัจจุบัน" ทั้งชุด ไม่ใช่แค่ "อะไรใหม่" — `last_seen_id` (ส่งไปกับ query string ทุกครั้ง) มีหน้าที่บอกว่ารายการไหนควรติด class `feed-item-new` (fade-in highlight) ในชุด HTML ที่ render กลับมา
    - **UI แบบ "บอลลูนแชท"** (`assets/feed.css`) — ปรับหลายรอบตามที่ขอ: (1) บอลลูนกว้างพอดีกับข้อความจริง (`max-width: 85%`, ไม่ยืด `flex:1` เท่ากันทุกแถว) (2) เวลาอยู่**นอก**บอลลูน ไม่ใช่ในตัวบอลลูนเอง ต่างจาก pattern ทั่วไปของ WhatsApp/iMessage (ที่ฝังเวลาไว้ในบอลลูน) ตามที่ตัดสินใจเลือกเอง (3) เวลาชิดแนวเส้นล่างของบอลลูน (`align-items: flex-end` ไม่ใช่ `center`) (4) ไม่มี `.card` ใหญ่ครอบทั้งลิสต์ (ตัดออกจาก `feed.php`) แต่ละบอลลูนใช้ `--card-bg` วางตรงบน `--page-bg` เลย
    - `recordPageview('feed')` เรียกจาก `feed.php` เข้าระบบสถิติ (Phase 9) เหมือนหน้าสาธารณะอื่น — `page_type='feed'` ไม่ต้องแก้ schema เพิ่มเพราะออกแบบให้รองรับหน้าใหม่ในอนาคตไว้แล้ว
    - **จำนวนข้อความที่โชว์ปรับได้** — เดิม hardcode `50` ใน 3 จุด (`feed.php`, `manage-feed.php`, `api/feed-poll.php`) ย้ายมาอ่านจาก `siteSetting('feed_item_limit', 50)` ที่เดียว ตั้งค่าได้ที่ `settings.php` (validate เป็นตัวเลข > 0 แบบเดียวกับ `articles_per_page`)
    - เพิ่มลิงก์ "จัดการฟีดข่าว" ใน `admin.php` dashboard + `adminNavGroups()` (sidebar แอดมิน) พร้อม badge จำนวนจาก `countFeedItems()` — เมนูหลักสาธารณะ ("Feed" ในภาพตัวอย่างที่ผู้ใช้ทดสอบ) เพิ่มเองผ่าน `menu.php` ที่มีอยู่แล้ว ไม่ต้องแก้โค้ดเพราะเมนูเป็น data-driven อยู่แล้ว

- **งานปรับปรุงย่อยรอบ 2026-07-28 (หลายจุด ไม่ผูก Phase เดียว)**
  - **`partials/header.php`: เพิ่ม page-level opt-in flag อีก 2 ตัว** ต่อยอด pattern เดิม (`$showSidebar`/`$showAdminSidebar`) — หน้าไหนอยากได้พฤติกรรมต่างจาก default แค่ตั้งตัวแปรก่อน `include` ไม่ต้องแก้ header.php เพิ่มอีก:
    - `$containerWide` — เมื่อไม่มี sidebar (`$hasSidebar` false) ให้เลือกได้ว่าจะใช้ `.container` (960px, ค่าเริ่มต้น) หรือเติม class `container-wide` (1140px เท่ากับ topbar/footer) — ใช้ครั้งแรกที่ `hello.php`
    - `$showMenu` — ซ่อนเฉพาะแถบเมนู (`.topbar-nav-row` เดสก์ท็อป + hamburger/accordion มือถือ) โดยที่แถบแบรนด์ (โลโก้/ชื่อเว็บ/search/theme toggle) ยังโชว์ตามปกติ — ตอนแรกออกแบบเป็น `$showTopbar` ซ่อนทั้งบล็อกรวมโลโก้ด้วย แต่พบว่าซ้ำซ้อนกับ pattern ที่มีอยู่แล้ว: หน้าที่อยากได้ chrome ว่างเปล่าทั้งหมดใช้วิธี "ไม่ `include partials/header.php` เลย" อยู่แล้ว (ดูตัวอย่างจริงที่ `error.php`'s fallback path บรรทัด ~39-60 ที่ hand-roll `<html>`/`<body>`/topbar เองตอน DB ล่ม) เลยตัดสินใจไม่เพิ่ม flag ซ้ำสำหรับ "ซ่อนทั้งหมด" — เก็บไว้แค่ granularity ที่ยังไม่มีทางอื่นทำได้ (ซ่อนแค่เมนู เก็บโลโก้ไว้)
    - `hello.php` เพิ่ม `recordPageview('other')` เข้าระบบสถิติ (Phase 9) ด้วย ตามแพทเทิร์นเดียวกับ `articles.php`/`category.php` ฯลฯ (ก่อนหน้านี้เป็นหน้าเดียวที่ include `partials/header.php` แล้วไม่นับ pageview เลย)
  - **ระบบลบไฟล์อัปโหลดที่ไม่ได้ใช้แล้วจริง (feature ใหม่)** — `includes/uploads.php` (ไฟล์ใหม่): `uploadPathInUse(string $path): bool` เช็คว่า path ยังถูกอ้างอิงอยู่ไหมทั้งใน `mblog_articles` (`featured_image` หรือ `content`) และ `mblog_sidebar_items` (`image` หรือ `content`) เช็คครบทั้ง 2 ตารางเสมอไม่ว่าจะลบจากฝั่งไหน เพราะ articles/sidebar items ใช้ `uploads/` พูลเดียวกัน — `deleteUploadIfUnused(string $path): void` `unlink()` ไฟล์จริง**เฉพาะ**เมื่อ `uploadPathInUse()` ตอบว่าไม่มีที่ไหนใช้แล้ว (mirror คำเตือนเดิมของ `syncArticleImages()`: "an orphaned row is cheap to fix, a deleted file used elsewhere isn't" — ต่างกันตรงที่ตรงนี้เช็คแล้วจริงๆ ก่อนแตะไฟล์)
    - เชื่อมเข้า `api/save.php` (บรรทัด ~108-117) และ `api/save-sidebar-item.php` (บรรทัด ~53-56): เทียบค่า `featured_image`/`image` เก่ากับใหม่หลัง UPDATE/INSERT เสร็จแล้ว ถ้าเปลี่ยน/ลบ เรียก `deleteUploadIfUnused()` กับค่าเก่า — ต้องเช็ค**หลัง**เขียน DB เสมอ (ไม่ใช่ก่อน) เพราะถ้ารูปเดิมยังถูกฝังซ้ำในเนื้อหาใหม่ของบทความ/ไอเทมเดียวกันเอง การเช็คหลังเขียนจะเห็น state สุดท้ายที่ถูกต้อง (ไม่ต้อง exclude self เพราะ query เห็น final state อยู่แล้ว)
    - **ขอบเขตที่ตั้งใจไม่ทำ**: ลบเฉพาะช่อง featured image/sidebar image เท่านั้น — รูปที่ถูกลบออกจาก**เนื้อหาบทความ** (Quill/rich text) ยังไม่ลบไฟล์อัตโนมัติ (ยังเป็นแค่ bookkeeping ผ่าน `mblog_images`/`syncArticleImages()` เหมือนเดิม — คนละกลไก คนละการตัดสินใจ)
    - ทดสอบยืนยันครบ 3 กรณี (ผ่านสคริปต์ทดสอบชั่วคราว ไม่กระทบข้อมูลจริง): ลบรูปที่ไม่มีใครใช้ซ้ำ → ไฟล์หายจริง; รูปเดียวกันใช้ 2 sidebar item ลบจากอันแรกอันเดียว → ไฟล์ยังอยู่ ลบจากอันที่สองด้วย → ไฟล์หายจริง; บทความที่ featured_image ซ้ำกับรูปที่ฝังในเนื้อหา (inline `<img>`) ลบ featured_image → ไฟล์ยังอยู่ (เนื้อหายังอ้างถึง)
  - **`featured_image`/`image`: validate แล้วตัดสินใจย้อนกลับเป็นแค่ `trim()`** — เริ่มจากบั๊กจริง: ฟีเจอร์ "ใส่ URL แทน" ใน editor พรีวิว URL ภายนอกสำเร็จฝั่ง browser (`new Image()`) แต่ `api/save.php`/`api/save-sidebar-item.php` เดิม validate ด้วย regex รับแค่ path แบบ `uploads/...` เท่านั้น (`// never trust an arbitrary URL into og:image`) ทำให้ URL ภายนอกถูกเคลียร์เป็นค่าว่างเงียบๆ ทุกครั้งตอนบันทึกจริง โดยไม่มี error ใดๆ — แก้รอบแรกด้วย `sanitizeFeaturedImagePath()` ใน `includes/uploads.php` รับทั้ง path แบบเดิม หรือ URL ภายนอกที่ผ่าน `filter_var(FILTER_VALIDATE_URL)` + จำกัด scheme `http(s)` เท่านั้น (กัน `javascript:`/`data:`)
    - **ตัดสินใจย้อนกลับให้ง่ายลงอีก**: หลังพิจารณาว่า `articleFeaturedImage()` (`includes/articles.php` บรรทัด ~234-259) เอง **ก็สร้าง/ใช้ URL ภายนอกเต็มรูปแบบ** (`https://img.youtube.com/vi/{id}/hqdefault.jpg`, ดึงจาก content ที่ไม่เคย validate ตอนบันทึกอยู่แล้ว) โดยไม่ validate อะไรเลย มาตั้งแต่แรก — validation ที่เพิ่งเพิ่มไปเลยกลายเป็น**เข้มกว่า field อื่นในระบบเดียวกันที่มีความเสี่ยงเท่ากันโดยไม่มีเหตุผลรองรับ** (output ก็ `htmlspecialchars()` ครอบอยู่แล้วทุกจุดที่พิมพ์ออก, จุดเดียวที่ต้องระวังจริงคือ `deleteUploadIfUnused()` ที่ `unlink()` ไฟล์ ซึ่งเช็ค pattern ของตัวเองก่อนเสมออยู่แล้วไม่ว่า input ตอน save จะเป็นอะไร) — ตัดกลับเหลือ `sanitizeFeaturedImagePath()` แค่ `trim($raw)` เท่านั้น ให้ trust level เดียวกับ `content`/YouTube-thumbnail fallback ที่มีอยู่แล้ว — **หลักการที่สรุปได้จากรอบนี้**: validate ให้ตรงจุดเสี่ยงจริง (จุดที่ค่านั้นถูกเอาไปทำสิ่งเสี่ยงจริง เช่นแตะ filesystem) ไม่ใช่ validate ที่จุดรับเข้าเพราะ "ดูน่าจะปลอดภัยกว่า" — ต้องเทียบกับ trust level ของ field อื่นที่มีความเสี่ยงเท่ากันในระบบเดียวกันก่อนเสมอ กันเกิด inconsistency ที่ไม่มีประโยชน์
  - **บั๊กที่เจอและแก้แล้ว: `mblog_pageview_log.page_path` แคบเกินไปสำหรับ slug ภาษาไทย** — คอลัมน์เดิม `varchar(255)` แต่ `recordPageview()` เก็บ `$_SERVER['REQUEST_URI']` เต็ม ซึ่งเป็น URL percent-encoded — ตัวอักษรไทย 1 ตัวกลายเป็น ~9 ตัวอักษรตอน encode (`%E0%B8%AA` ฯลฯ) ทำให้ URL ของบทความ slug ภาษาไทยยาวเกิน 255 ได้ง่ายมาก (พบจริงที่ 477 และ 600 ตัวอักษร) — MySQL (strict mode) ปฏิเสธทั้งแถว insert (`SQLSTATE[22001]: String data, right truncated`) ซึ่งโดน `try/catch` ใน `recordPageview()` ครอบไว้ (ดู Phase 9) ทำให้หน้าเว็บโหลดปกติไม่มี error ให้เห็นเลย แต่ pageview/`view_count` ไม่ถูกนับเลยเงียบๆ — ขยายคอลัมน์เป็น `varchar(500)` ก่อน (พบ log error `Data too long` ซ้ำสำหรับ slug ที่ยาวกว่านั้นอีก) แล้วขยายเป็น **`TEXT`** ในที่สุด (ไม่มี index ผูกกับคอลัมน์นี้เลย เปลี่ยนได้อิสระ ไม่ต้องมาไล่ขยายเป็นตัวเลขคงที่ซ้ำอีกไม่ว่า slug จะยาวแค่ไหนในอนาคต)
  - **`partials/article-list.php`: เพิ่มยอดวิวในการ์ดรายการ** — แถว `.meta` เดิมมีแค่ `relativeTimeTag()` เปลี่ยนเป็น `การดู <?= number_format(articleViewCount($a['id'])) ?> ครั้ง • <?= relativeTimeTag(...) ?>` — เพิ่ม `require_once includes/stats.php` ในตัว partial เองด้วย (ไม่ใช่ทุกหน้าที่เรียก partial นี้ require มาก่อน เช่น `pages.php`/`drafts.php`) กันเกิด "Call to undefined function" — มีผลครบ 5 หน้าที่ใช้ partial ร่วมกัน (`articles.php`/`pages.php`/`category.php`/`tag.php`/`drafts.php`) พร้อมกันทันที — หมายเหตุ: `pages.php`/`page.php` จะโชว์ "การดู 0 ครั้ง" เสมอ เพราะ `page.php` เรียก `recordPageview('page')` โดยไม่ส่ง `$articleId` เข้าไป (ไม่เคยนับแยกรายหน้าตั้งแต่ต้น ไม่ใช่บั๊กใหม่จากงานนี้)
  - **`assets/editor.js`: เพิ่ม keyboard navigation ให้ช่องแท็ก (`setupTagInput()`)** — เดิม `keydown` listener (บรรทัด ~668 เดิม) จัดการแค่ `Enter`/`,`/`Backspace` **ไม่มีโค้ดจัดการ `ArrowDown`/`ArrowUp` เลยสักบรรทัด** และการเลือกจากรายการแนะนำทำได้ทาง `mousedown` เท่านั้น (ไม่มีแนวคิด "highlighted item" ในโค้ดเลย) — เพิ่ม `currentMatches`/`activeIndex` เก็บ state ของรายการแนะนำ, `setActiveIndex()`/`hideSuggestions()`, และ handler ให้ `ArrowDown`/`ArrowUp` (ไล่ highlight, wrap รอบ), `Escape` (ปิดรายการ), `Enter` เลือก suggestion ที่ highlight ก่อนถ้ามี ไม่งั้น fallback เป็นพฤติกรรมเดิม (เพิ่มข้อความดิบที่พิมพ์) — เพิ่ม class `.tag-suggestion-item.active` ใน `assets/editor.css` ใช้สีพื้นหลังเดียวกับตอน hover
    - **บั๊กที่เจอและแก้แล้วระหว่างทดสอบ**: เพิ่มโค้ดแล้วผู้ใช้ยังกด ArrowDown ไม่ได้ผล — สาเหตุจริงคือ `<input type="text" id="tag-input">` ที่ `editor.php` **ไม่มี `autocomplete="off"`** เบราว์เซอร์เลยโชว์ autofill dropdown ของตัวเองทับ/แข่งกับ dropdown ที่สร้างเอง (ถ้าเคยพิมพ์แท็กในช่องนี้มาก่อนในเบราว์เซอร์นั้น) พอกด ArrowDown เบราว์เซอร์เอาคีย์ไปเลื่อน list ของตัวเองก่อน ไม่ปล่อยให้ JavaScript เห็น event เลย ทำให้โค้ดที่เพิ่งเพิ่มไปดูเหมือน "ใช้งานไม่ได้" ทั้งที่ logic ถูกต้อง (ยืนยันแล้วด้วย synthetic keyboard event ผ่าน JS ว่า arrow-key/highlight/Enter ทำงานถูกทุกกรณีเมื่อไม่มี autofill มาแย่ง) — แก้ด้วยการเพิ่ม `autocomplete="off"` ที่ `editor.php` บรรทัด input นี้
  - **`searchArticles()` (`includes/articles.php`): ค้นหาสาธารณะเพิ่มการ match แท็กด้วย** — เดิมค้นแค่ `title`/`content` เท่านั้น ตอนนี้เพิ่ม `OR EXISTS (SELECT 1 FROM mblog_article_tag mat JOIN mblog_tags t ON t.id = mat.tag_id WHERE mat.article_id = a.id AND t.name LIKE ?)` ทั้งใน COUNT query และ SELECT query — ใช้ `EXISTS` แทน `JOIN mblog_tags` ตรงๆ โดยตั้งใจ เพราะแท็กเป็น many-to-many (1 บทความมีได้หลายแท็ก) ถ้า join ตรงๆ บทความที่มีหลายแท็กตรงคำค้นจะได้แถวซ้ำ กระทบทั้ง `COUNT(*)` และ pagination — ให้คะแนน relevance ของ tag match = 2 (เท่ากับ title, ตามเหตุผลที่ผู้ใช้ให้ไว้ว่าแท็กเป็นคำที่คนตั้งใจติดไว้ บ่งบอกตรงกว่า content) **ตัดสินใจไม่รวม category** เข้าเงื่อนไขค้นหา แม้จะ join อยู่แล้ว (1:1 ไม่มีปัญหาซ้ำแถว) เพราะหมวดหมู่เป็นกลุ่มกว้างกว่ามาก คำค้นทั่วไปที่บังเอิญตรงชื่อหมวดจะดึงผลลัพธ์ที่ไม่เกี่ยวข้องมาเยอะเกินจำเป็น — ทดสอบยืนยันด้วยข้อมูลจริง: บทความที่ติดแท็ก "มือใหม่" แต่ไม่มีคำนี้ใน title/content เลย ตอนนี้โผล่ในผลค้นหาแล้ว เรียงถูกต้องตาม relevance แล้ว fallback `published_at DESC`, ไม่มีปัญหาแถวซ้ำ/pagination พัง

- **ฐานข้อมูล MySQL (`mblog`)** — ตาราง `mblog_*` ทั้งหมด 14 ตัว (คอลัมน์เต็มดู `database/*.sql`)
  - ใช้งานจริงแล้ว (Phase 1): `mblog_articles` (มีคอลัมน์ `type` แยก post/page), `mblog_images`, `mblog_categories` (มีคอลัมน์ `color` เก็บโทนสี badge), `mblog_menu_items`, `mblog_slug_redirects`
  - ใช้งานจริงแล้ว (Phase 4): `mblog_tags`, `mblog_article_tag`
  - ใช้งานจริงแล้ว (นอกแผน Phase เดิม — ย้ายจากไฟล์ตอนทำหน้าแอดมิน settings.php): `mblog_settings`
  - ใช้งานจริงแล้ว (นอกแผน Phase เดิม — ระบบ sidebar item, `database/sidebar.sql`): `mblog_sidebar_items`
  - ใช้งานจริงแล้ว (Phase 9, ทำเสร็จ 2026-07-27): `mblog_article_stats` (ยอดรวมตลอดกาลต่อบทความ), `mblog_pageview_log` (log ราย pageview ทั้งเว็บ — ดูหัวข้อ "ระบบสถิติ" ด้านบน)
  - ใช้งานจริงแล้ว (นอกแผน Phase เดิม — ระบบฟีดข่าวสั้น, `database/feed_items.sql`, ทำเสร็จ 2026-07-27): `mblog_feed_items`
  - ใช้งานจริงแล้ว (Phase 3 + Phase 15, ทำเสร็จ 2026-08-06): `mblog_staff` (ทีมงาน — ชื่อเดิมตอนสร้างคือ `mblog_users` สลับชื่อทีหลัง ดูหัวข้อ "ระบบล็อกอิน/สิทธิ์" ด้านล่าง)
  - เพิ่มคอลัมน์โปรไฟล์แล้วแต่ยังไม่มีหน้าเว็บใช้งาน (Phase 15): `mblog_users` (ผู้ชมทั่วไป — ชื่อเดิมตอนสร้างคือ `mblog_readers`)
  - สร้างตารางไว้รอล่วงหน้าแล้ว แต่ยังไม่มีโค้ดใช้งาน (Phase 7): `mblog_comments`

- **ฟีเจอร์ตัวเขียน (เทียบเท่า WP editor พื้นฐาน)**
  - หัวข้อ H1–H3, ตัวหนา/เอียง/ขีดเส้นใต้, สีตัวอักษร, จัดกึ่งกลาง/ซ้าย/ขวา/เต็มบรรทัด
  - Bullet list และ numbered list
  - แทรกรูปภาพ + ลากปรับขนาด (มุมขวาล่าง) + จัดตำแหน่งรูปซ้าย/กลาง/ขวา (ซ้าย/ขวาแบบ float ให้ข้อความไหลรอบ)
  - คำบรรยายใต้ภาพ (caption)
  - ลิงก์บนข้อความ และลิงก์บนรูปภาพ (ผ่าน mini-toolbar ลอยเวลาเลือกรูป)
  - **Auto-link** — พิมพ์/วาง URL (http, https, www.) แล้วกลายเป็นลิงก์ให้อัตโนมัติ (มี safety-net ตอนบันทึกด้วย เผื่อ paste URL ท้ายสุดไม่มีช่องว่างตาม)
  - Blockquote, Code block พร้อม **syntax highlighting สไตล์ GitHub (ธีมสว่าง)** ผ่าน highlight.js + ปุ่ม "คัดลอก" มุมขวาบนแบบ GitHub
  - เส้นคั่นบทความ (divider/hr)
  - **Featured image เลือกได้ 2 แบบ** — อัปโหลดไฟล์ หรือใส่ URL ภาพตรงๆ (validate ด้วยการโหลดรูปทดสอบก่อน commit)
  - **Mobile responsive** — ปรับ viewport, ยุบ float ของรูปซ้าย/ขวาเป็นเต็มความกว้างบนจอเล็ก, รองรับลากปรับขนาดรูปด้วยนิ้ว (touch), ปรับขนาดตัวอักษร/ปุ่มให้เหมาะจอมือถือ

- **โครงสร้างประกอบหน้า header/sidebar/footer แยกเป็น 3 ไฟล์จริง (Phase 13, ทำเสร็จ 2026-07-30 — ดู [PLANNING.md](PLANNING.md) หัวข้อ 15)**
  - `partials/sidebar.php` (ไฟล์ใหม่) — แยกออกมาจาก `footer.php` เดิม เหลือแค่หน้าที่ปิด `.main-content` + render `<aside class="sidebar">` เท่านั้น — `partials/footer.php` เดิมเหลือแค่ `</div>` ปิด container + `<footer>` จริง + script ท้ายหน้า
  - `includes/layout.php` (ไฟล์ใหม่) — `render_header(array $args = []): array`, `render_sidebar(array $args = []): void`, `render_footer(array $args = []): void` — แต่ละตัว `extract($args)` ก่อน `require` partial ที่เกี่ยวข้อง แทนการ `include` ตรงๆ แบบเดิม — **`render_header()` คืนค่า array `compact('hasSidebar', 'showAdminSidebar', 'sidebarItems')`** เพราะตัวแปร 3 ตัวนี้เดิมคำนวณอยู่ใน `header.php` แล้ว "รั่ว" เข้า scope ของไฟล์หน้าเว็บเองผ่าน `include` (PHP `include` แชร์ scope กับไฟล์ที่เรียก) — พอเปลี่ยนเป็นเรียกผ่านฟังก์ชัน ตัวแปรพวกนี้กลายเป็น local ของ `render_header()` เท่านั้น ต้องส่งกลับมาเป็น return value แล้วให้หน้าเว็บส่งต่อเข้า `render_sidebar($layout)` เอง — `require_once __DIR__ . '/includes/layout.php';` อยู่ใน `config.php` (จุดเดียวที่ทุกหน้าเรียกผ่านอยู่แล้วไม่ว่าทางตรง/อ้อม เลยการันตีว่า 3 ฟังก์ชันนี้พร้อมใช้เสมอ)
  - **22 ไฟล์หน้า** ที่เคย `include partials/header.php`/`footer.php` ตรงๆ แก้เป็นเรียก `render_header()`/`render_sidebar($layout)`/`render_footer()` แบบ explicit ทั้งหมดแล้ว รวม `error.php` (try/catch ที่ครอบการ render chrome เต็มรูปแบบไว้ยังทำงานถูกต้อง เพราะ exception จาก `render_header()` โยนทะลุออกจากฟังก์ชันมาได้ปกติเหมือน `include` เดิม) และ `includes/manage-list.php`'s `renderManageListPage()` (เป็นฟังก์ชันอยู่แล้ว ไม่กระทบการออกแบบนี้เลย เพราะ `$layout` เป็นแค่ local variable ธรรมดาไม่ว่าจะอยู่ใน scope ไหน)
  - เทสยืนยันแล้วว่าทุก layout เดิมทำงานเหมือนเดิมทุกกรณี: หน้า 404 (chrome เต็มผ่าน error.php), หน้ารายการ sidebar ขวา, หน้าแอดมิน sidebar ซ้าย, หน้า editor, หน้าจัดการบทความ, หน้าบทความจริง

- **ฟีเจอร์บทความเพิ่มเติม: SEO override / Sticky / Scheduled publishing / Post expiration (Phase 11, ทำเสร็จ 2026-07-30 — ดู [PLANNING.md](PLANNING.md) หัวข้อ 13)**
  - **SEO override ต่อบทความ** — คอลัมน์ `seo_title`/`seo_description`/`seo_noindex` บน `mblog_articles` (NULL/0 = auto-generate เหมือนเดิม) — `articleSeoTitle()`/`articleSeoDescription()` ใน `includes/articles.php` คืนค่า override ถ้ามี ไม่งั้น fallback ไปชื่อจริง/`autoArticleDescription()` — `article.php`/`page.php` ใช้ค่านี้กับ `<title>`, og:title/description, twitter:title/description, JSON-LD headline/description ทุกจุด + เติม `<meta name="robots" content="noindex,follow">` เมื่อติ๊ก noindex
    - **ลบคอลัมน์ `excerpt` ทิ้งแล้ว** — เดิม `excerpt` มีจุดใช้งานเดียวคือ fallback ให้ meta description พอมี `seo_description` มาทำหน้าที่เดียวกัน (priority สูงกว่าด้วย) `excerpt` เลยกลายเป็นข้อมูลที่กรอกแล้วไม่มีผลจริง — ก่อนลบ migrate ค่าที่มีอยู่จริง (11 แถวที่ไม่ว่างและ `seo_description` ยังว่าง) เข้า `seo_description` ก่อนด้วย `UPDATE ... SET seo_description = excerpt WHERE ...` แล้วค่อย `ALTER TABLE mblog_articles DROP COLUMN excerpt` — เอาฟิลด์ "สรุปสั้น" ออกจาก `editor.php`/`assets/editor.js`/`api/save.php` และ `includes/markdown-import.php` (จุดที่เคย insert `excerpt = ''` ทิ้งเปล่าๆ อยู่แล้ว) — เปลี่ยนชื่อ `articleExcerpt()` → **`autoArticleDescription()`** ใน `includes/articles.php` (ตัด branch เช็คค่า `$article['excerpt']` ที่ไม่มีทางเป็นจริงอีกแล้วออก เหลือแค่ auto-generate จาก content)
  - **Sticky/ปักหมุด** — เก็บเป็น JSON array `sticky_article_ids` ใน `mblog_settings` (ลำดับใน array คือลำดับแสดงผลจริง ไม่ใช่แค่ membership) — `includes/articles.php`: `getStickyArticleIds()`, `isStickyArticle()`, `pinArticle($id)`/`unpinArticle($id)` (เพิ่ม/ลบทีละตัว ไม่แตะตัวอื่น), `setStickyArticles($orderedIds)` (แทนที่ทั้ง set — ใช้เฉพาะกับ "รายการที่ปักหมุดอยู่ทั้งหมด" เท่านั้น ไม่ใช่ subset ที่กรองมา), `unstickArticles($ids)` (เรียกจาก `bulkPermanentlyDeleteArticles()` เคลียร์ id ที่ถูกลบถาวรออกจาก array อัตโนมัติ) — `getArticleList()` เพิ่ม param `bool $stickyFirst = false`, ตอน true จะ prefix `ORDER BY` ด้วย `CASE WHEN a.id IN (...) THEN 0 ELSE 1 END, FIELD(a.id, ...)` (inline ตรงๆ เพราะ id เป็น int-cast แล้ว ไม่มีความเสี่ยง injection) — `articles.php`/`pages.php` ส่ง `stickyFirst=true` (WHERE ของแต่ละ query กรอง type อยู่แล้ว จึงปลอดภัยที่ id ของ post จะไม่ไปโผล่ผิดที่ใน page list)
    - **หน้าจัดการ `sticky-items.php`** (ใหม่) — ไม่ใช่ตารางรวมบทความทั้งหมดแบบที่ร่างไว้ทีแรก (จะช้าเมื่อบทความเยอะ + มีบั๊กจริงถ้าผสม search filter เข้าไปในฟอร์มเดียวกัน — บันทึกตอนกรองอยู่จะลบปักหมุดของบทความนอกผลค้นหาทิ้งไปด้วย) แบ่งเป็น 2 ส่วนแยกกันเด็ดขาด: (1) **ค้นหาเพื่อปักหมุดเพิ่ม** — กล่องค้นหา (GET) + ผลลัพธ์มีปุ่ม "ปักหมุด" ต่อแถว กดแล้วยิง POST เดี่ยวๆ เรียก `pinArticle()` ทันที ไม่ผูกกับ state ของแถวอื่น (2) **รายการที่ปักหมุดอยู่** — ตารางเล็กแสดงเฉพาะที่ปักหมุดจริง (จำนวนน้อยเสมอ) มีช่องกรอกลำดับ + ปุ่ม "ถอดหมุด" ต่อแถว, "บันทึกลำดับ" ทำงานกับ set นี้เท่านั้นซึ่งสมบูรณ์เสมอ (ไม่มีทางถูกกรองบางส่วน) — เพิ่ม filter `search` ให้ `getArticleList()` (`a.title LIKE ?`) รองรับกล่องค้นหานี้
    - `includes/manage-list.php` (`manage-articles.php`/`manage-pages.php`) — เอาปุ่มปักหมุด/ถอดหมุดที่เคยฝังในตารางออกแล้ว เหลือแค่ไอคอน 📌 ข้างชื่อบทความ/หน้าที่ปักหมุดอยู่ (read-only, บอกสถานะเฉยๆ ไม่ใช่ปุ่มกด)
  - **Scheduled publishing + Post expiration** — ไม่มี cron เลย ใช้การเทียบเวลาตอน query แทนตามที่ตัดสินใจไว้แต่แรก: `publicVisibilitySql(string $alias = 'a'): string` ใน `includes/articles.php` คืน SQL fragment `"$alias.status IN ('published','scheduled') AND $alias.published_at <= NOW() AND ($alias.expires_at IS NULL OR $alias.expires_at > NOW())"` — จุดเดียวที่นิยาม "เผยแพร่จริงต่อสาธารณะ" เดินสายผ่าน `getArticleList()` (แทนที่ `a.status = ?` เดิมเมื่อ filter คือ `'published'`), `searchArticles()`, `getArticle()`, `getPage()` — **ไม่แตะ** `getArticlesForAdmin()`/`getArticleStatusCounts()` (แอดมินต้องเห็น/จัดการบทความที่ยังไม่ถึงเวลา/หมดอายุแล้วได้)
    - `editor.php` — ปุ่มที่ 3 "ตั้งเวลาเผยแพร่" (validate ฝั่ง JS ต้องเป็นอนาคตก่อนส่ง) คู่กับ `<input type="datetime-local" id="scheduled-at">`, ช่อง `expires-at` แยกต่างหาก (ไม่บังคับ, ใช้ได้กับทุกสถานะไม่ใช่แค่ scheduled) — badge สถานะเพิ่ม "ตั้งเวลา" (`.status-scheduled`, สีฟ้าจาก `--tag-blue-*` ที่มีอยู่แล้ว ไม่เพิ่มสีใหม่)
    - `api/save.php` — `$status` รับ 3 ค่า (`draft`/`published`/`scheduled`) — `published_at`: ถ้า scheduled ใช้ค่าจาก `scheduled_at` ที่แอดมินเลือก (fallback เป็นตอนนี้ถ้า parse ไม่ได้), ถ้า published และ `published_at` เดิมเป็น**อนาคต** (คือกำลัง publish ทับ schedule ที่ตั้งไว้) จะรีเซ็ตเป็นตอนนี้ (คลิก "เผยแพร่" ควรแปลว่า "เอาขึ้นทันที" เสมอ ไม่ใช่ค้างเวลาเดิมที่เคยตั้งไว้) — republish ปกติ (ที่ `published_at` เดิมเป็นอดีตอยู่แล้ว) ยังคงค่าเดิมไว้เหมือนเดิม
    - `includes/manage-list.php` — badge "ตั้งเวลา" ในตารางแอดมินโชว์วันที่/เวลาที่ตั้งไว้ใต้ badge ด้วย

- **Backup ระบบเต็มรูปแบบ (ครอบคลุม MySQL แล้ว, ทำเสร็จ 2026-08-01 — ดู [PLANNING.md](PLANNING.md) หัวข้อ 9/11)**
  - `includes/backup.php` (ไฟล์ใหม่) — logic กลาง ใช้ร่วมกันทั้ง `scripts/backup.php` (CLI/cron) และหน้าแอดมิน `backup.php` (ปุ่มกด) เหมือน pattern `includes/feed.php`/`includes/markdown-import.php` เดิม
    - `getBackupTables()` — อ่านรายชื่อตารางจริงด้วย `SHOW TABLES` (ไม่ hardcode array) ลบ `mblog_pageview_log` ออก (`BACKUP_EXCLUDED_TABLES`) เพราะโตเร็วและยังไม่มีนโยบาย retention/rollup ตัดสินใจ — ตารางใหม่จาก migration ในอนาคตจะถูกรวมอัตโนมัติโดยไม่ต้องแก้โค้ดนี้เลย
    - `buildDatabaseDumpSql()` — dump ด้วย **PHP ล้วน** ต่อตาราง: `SHOW CREATE TABLE` + `SELECT *` แล้วสร้าง `INSERT` เอง (ค่าทุกตัวผ่าน `PDO::quote()`, `NULL` handle แยก) ครอบทั้งไฟล์ด้วย `SET FOREIGN_KEY_CHECKS=0/1` กันปัญหาลำดับตารางตอน restore — **ตัดสินใจไม่ใช้ `mysqldump` ผ่าน `shell_exec()`** เพราะยังไม่รู้ spec โฮสจริงตอน deploy และโฮสแชร์หลายเจ้าปิด `exec`/`shell_exec` ไว้ (pure-PHP รันได้แน่นอนทุกที่) — `mysqldump` ยังเป็นทางเลือกในอนาคตถ้าข้อมูลโตจนโหลดทั้งตารางเข้า memory ทีเดียวไม่ไหว หรืออยากได้ `--single-transaction` (dump ของเราไม่ครอบ transaction ข้ามตาราง แต่ traffic เว็บนี้ต่ำมาก ความเสี่ยงจึงต่ำ)
    - `createBackupArchive()` — zip เดียวรวม `db-dump.sql` (`ZipArchive::addFromString`, สร้างสดไม่เขียนลงดิสก์ก่อน) + ทุกไฟล์ใน `uploads/` (recursive iterator เดิมจาก backup script ตัวเก่า) — คืน path/filename/size ให้ทั้ง CLI และหน้าเว็บใช้ต่อ
    - `pruneOldBackups()` — retention logic เดิม (ลบไฟล์เก่ากว่า `RETENTION_DAYS`) แยกออกมาเป็นฟังก์ชันเฉยๆ ไม่เปลี่ยนพฤติกรรม
    - `ensureBackupDir()` — **self-provision `backups/.htaccess`** (deny-all ทั้ง `Require all denied`/`Deny from all` รองรับ Apache 2.2+2.4) ตอนสร้างโฟลเดอร์ครั้งแรก เพราะ `.gitignore` ตัด `backups/` ทั้งโฟลเดอร์ทิ้ง (ต่างจาก `uploads/` ที่มี `.gitkeep`) เขียนผ่าน git ไม่ได้เลยไม่ว่าจะ deploy วิธีไหน ต้องให้โค้ดจัดการเองตอน runtime — ยืนยันแล้วว่า URL ตรงไปที่ไฟล์ zip return 403 จริง
    - `streamBackupDownload()`/`deleteBackupFile()` ใช้ `resolveBackupPath()` ร่วมกัน validate ชื่อไฟล์ด้วย regex `^backup-\d{8}-\d{6}\.zip$` + `realpath()` containment check ก่อนแตะ filesystem ทุกครั้ง — ไม่มีช่องทาง path traversal (ทดสอบยิง `?download=../config.php` แล้ว ถูกปฏิเสธ ไม่กระทบไฟล์จริง)
  - `scripts/backup.php` — เดิมมี logic zip ในตัวเองทั้งหมด ตอนนี้เหลือแค่ CLI wrapper บางๆ (`require includes/backup.php` แล้วเรียก `createBackupArchive()`/`pruneOldBackups()`) คอมเมนต์ cron line บนสุดยังอยู่เหมือนเดิม
  - **`backup.php` (หน้าแอดมินใหม่)** — ปุ่ม "Backup ตอนนี้" (POST + PRG redirect แบบเดียวกับ `manage-feed.php`) + ตาราง `admin-table` ลิสต์ backup ที่มี (ชื่อ/ขนาด/เวลาแบบ `relativeTimeTag()`) พร้อมลิงก์ดาวน์โหลดและปุ่มลบ (`confirm()` ก่อนลบ เหมือน `manage-feed.php`) — เพิ่มเข้า `adminNavGroups()`/`admin.php` dashboard การ์ดในกลุ่ม "ตั้งค่าเว็บ" แล้ว
  - **บั๊กที่เจอและแก้แล้วระหว่างทดสอบ**: กดปุ่ม "Backup ตอนนี้" ผ่านเบราว์เซอร์แล้ว `ZipArchive::close()` throw "Failure to create temporary file: Permission denied" — สาเหตุคือ `backups/` ถูกสร้างครั้งแรกจากฝั่ง CLI (`mkdir(..., 0755)`, เจ้าของไฟล์เป็น shell user) แต่ Apache รันเป็น user `daemon` (เห็นได้จาก `httpd.conf`) เขียนเข้าโฟลเดอร์ที่ owner-only-write ไม่ได้ — คนละ user เขียนโฟลเดอร์เดียวกันเป็นปัญหาเดิมที่ `uploads/` เคยเจอมาแล้ว (ดูหัวข้อบั๊กด้านล่าง) แก้ด้วยวิธีเดียวกัน: เปลี่ยน `ensureBackupDir()` ให้ `mkdir(..., 0777, true)` แทน — ทดสอบยืนยันแล้วว่า trigger ผ่านเว็บสร้างไฟล์ที่เป็นเจ้าของโดย `daemon` ได้จริงหลังแก้
  - **ทดสอบ restore จริงแล้ว** — import `db-dump.sql` จาก backup ที่สร้างจริงเข้า database ทดสอบแยกต่างหาก เทียบจำนวนแถวกับของจริงตรงกันทุกตาราง (articles/categories/tags/feed_items/users) ไม่มี SQL error เลย

- **ระบบกำจัดไฟล์กำพร้า (ทำเสร็จ 2026-08-01 — ดู [PLANNING.md](PLANNING.md) หัวข้อ 16/Phase 14)**
  - จุดเริ่ม: `syncArticleImages()`/`deleteUploadIfUnused()` (หัวข้อ 9) ลบไฟล์จริงเฉพาะตอน featured image เปลี่ยน — รูปที่ถูกลบออกจาก**เนื้อหาบทความ** (rich text) ตั้งใจไม่ลบไฟล์อัตโนมัติ (กัน false positive จาก regex parse `<img>`) ทำให้ไฟล์เหล่านั้นค้างถาวรใน `uploads/` ถ้าไม่มีระบบมากวาดทีหลัง
  - **บั๊กแฝงที่เจอและแก้แล้วก่อนเริ่มทำฟีเจอร์นี้จริง**: `uploadPathInUse()` (`includes/uploads.php`) เดิมเช็คแค่ `mblog_articles`/`mblog_sidebar_items` **ไม่เคยเช็ค `mblog_settings.site_logo`/`site_favicon` เลย** (ยืนยันจากค่าจริงในตาราง: `site_logo` → `uploads/site/logo.png`, `site_favicon` → `uploads/site/favicon.ico`) — ถ้าไม่แก้ก่อน scanner ใหม่จะรายงานโลโก้/favicon เว็บเป็นไฟล์กำพร้าปลอมๆ (เสี่ยงโดนแอดมินติ๊กลบถาวร) และเป็นบั๊กแฝงที่มีอยู่แล้วจริงใน `deleteUploadIfUnused()` เดิมด้วย (เรียกจาก `api/save.php` ตอนเปลี่ยน featured image — ถ้าบังเอิญ path ตรงกับโลโก้ ก็เสี่ยงลบเงียบๆ ได้เหมือนกัน) — แก้ที่ต้นตอ: เพิ่ม `require_once includes/settings.php` แล้วเช็ค `siteSetting('site_logo', '')`/`siteSetting('site_favicon', '')` เป็นเงื่อนไขที่ 3 ใน `uploadPathInUse()` ตรงๆ (ไม่มี circular require เพราะ `settings.php` require แค่ `db.php`) — ทั้งจุดเดิมและ scanner ใหม่ได้ผลถูกต้องพร้อมกันจากจุดแก้เดียว
  - `includes/orphan-files.php` (ไฟล์ใหม่)
    - `scanOrphanUploads()` — `RecursiveDirectoryIterator` วนทุกไฟล์จริงใน `uploads/` (โครงเดียวกับ `includes/backup.php`'s zip logic) ข้ามไฟล์ที่ขึ้นต้นด้วย `.` แบบ explicit (`.DS_Store`/`.gitkeep` พบจริงในโฟลเดอร์ — `FilesystemIterator::SKIP_DOTS` ข้ามแค่ `.`/`..` เท่านั้น ไม่ข้าม dotfile พวกนี้) แต่ละไฟล์เทียบผ่าน `uploadPathInUse()` ที่แก้แล้ว ไม่ผ่าน (ยังไม่ถูกใช้) → เป็น candidate เก็บ path/ขนาด/วันที่สร้าง (`filemtime()`)/`is_image` (เช็ค extension ไว้โชว์ thumbnail) เรียงจากไฟล์ใหญ่สุดก่อน (คืนพื้นที่ได้เยอะสุดให้เห็นก่อน)
    - `deleteOrphanFiles(array $relativePaths)` — **re-check `uploadPathInUse()` อีกครั้งตอนลบจริง** ไม่เชื่อผลจาก scan อย่างเดียว เพราะสแกน (GET) กับลบ (POST) เป็นคนละ request กัน ระหว่างนั้นอาจมีอะไรมาอ้างอิงไฟล์นั้นเพิ่มได้ — validate path ต้องขึ้นต้นด้วย `uploads/` + `realpath()` containment check ก่อนแตะ filesystem เสมอ (defense-in-depth แบบเดียวกับ `includes/backup.php`'s `resolveBackupPath()`) — ทดสอบยิง `../config.php` เข้า `paths[]` แล้วถูกปฏิเสธ ไม่กระทบไฟล์จริง
  - **`orphan-files.php` (หน้าแอดมินใหม่)** — bulk-checkbox UI (`#select-all` + `.row-select`, reuse `assets/manage-list.js` ที่มีอยู่แล้วสำหรับ select-all wiring — ฟังก์ชัน submit-handler เดิมของสคริปต์นี้ no-op เองอย่างปลอดภัยเพราะหน้านี้ไม่มี `#bulk-action-select` ให้มันหา) ต่างจาก `manage-list.php` ตรงที่ไม่มี form ซ้อน form เลย (ไม่มีปุ่มลบทีละแถวแยก) เลยไม่ต้องใช้ `form=""` attribute trick แบบที่นั่น — ตารางโชว์ thumbnail เล็กๆ (เฉพาะไฟล์รูป), path, ขนาด (reuse `formatBackupSize()` จาก `includes/backup.php`), วันที่ (`relativeTimeTag()`) — ปุ่ม "ลบไฟล์ที่เลือก" มี `confirm()` ก่อนเสมอ (ข้อความ static ไม่ต้องมีจำนวนแบบ dynamic เหมือน per-row delete ที่อื่นในระบบ) — เพิ่มเข้า `adminNavGroups()`/`admin.php` แล้ว (แยกหน้าต่างหาก ไม่รวมกับ `backup.php` เพราะเป็นคนละ action กัน)
  - **ตัดสินใจไม่ทำ**: safety-zip ไฟล์อัตโนมัติก่อนลบจริง (ใช้ `backups/` ที่มีอยู่แล้วได้) — ถ้าต้องการ safety net ให้ไปกด "Backup ตอนนี้" ที่ `backup.php` เองแยกต่างหากก่อน ไม่ผูกอัตโนมัติเข้าหน้านี้ (คนละ concern กัน เพิ่มความซับซ้อนโดยไม่จำเป็น)
  - **ทดสอบยืนยันด้วยข้อมูลจริง**: `scanOrphanUploads()` เจอไฟล์กำพร้าจริง 5 ไฟล์ในระบบ (cross-check กับ DB ตรงๆ ยืนยันว่าไม่มีที่ไหนอ้างอิงเลยจริง) โลโก้/favicon ไม่โผล่เป็น candidate, featured image ที่ใช้งานจริงอยู่ก็ไม่โผล่ — สร้างไฟล์ทดสอบ (วางไว้ที่ `uploads/` root เพราะ `uploads/2026/07/` เป็นเจ้าของโดย `daemon` เขียนจาก shell user ไม่ได้ — เจอปัญหาสิทธิ์เดียวกับที่เอกสารไว้ในหัวข้อบั๊กด้านล่างอีกรอบระหว่างเตรียมเทส) แล้วลบผ่าน request จริงเข้า `orphan-files.php` สำเร็จ ไฟล์หายจริงจากดิสก์ ไฟล์กำพร้าจริงอีก 5 ไฟล์ไม่ถูกแตะ

- **Sidebar item type: iframe embed (ทำเสร็จ 2026-08-01 — ดู [PLANNING.md](PLANNING.md) หัวข้อ 15 ส่วนต่อขยาย)**
  - จุดเริ่ม: Phase 13 (header/sidebar/footer restructure) ตั้งใจแยก "sidebar item 2 แบบ" ออกไปทำทีหลัง หลังแก้รากฐาน render ให้ถูกก่อน — รอบนี้กลับมาทำต่อจริง เพิ่ม type ที่ 2 คือ iframe embed (เดิมมีแค่ article-style: รูป/ข้อความ/ลิงก์)
  - **Schema** — `database/sidebar_item_type.sql` (migration ใหม่, additive ล้วนๆ): `ALTER TABLE mblog_sidebar_items ADD COLUMN type VARCHAR(20) NOT NULL DEFAULT 'article', ADD COLUMN iframe_src VARCHAR(500) NULL, ADD COLUMN iframe_height INT UNSIGNED NULL` — รายการเดิมทุกแถวได้ `type='article'` อัตโนมัติ พฤติกรรมเดิมไม่เปลี่ยนเลย — **เลือกคอลัมน์เฉพาะ ไม่ใช้ `meta` JSON** (ทางเลือกที่ PLANNING.md เคยทิ้งไว้ให้ตัดสินใจ) เพราะตรงกับที่ทุกตารางในระบบนี้ทำอยู่แล้ว (คอลัมน์ explicit ทั้งหมด เช่น `mblog_articles`'s `seo_title`/`expires_at`) และตอนนี้มีแค่ 2 type เท่านั้น ใช้ JSON จะเป็นการทำ abstraction ล่วงหน้าเกินความจำเป็น
  - **`includes/sidebar.php`** — `getActiveSidebarItems()` เพิ่ม `type`/`iframe_src`/`iframe_height` เข้า `SELECT` list (เดิมไม่ดึงมาเลย) — `createSidebarItem()`/`updateSidebarItem()` เพิ่ม param ใหม่ 3 ตัวแบบ positional ต่อจากของเดิม (ไม่เปลี่ยนเป็น array/DTO เพราะทุกฟังก์ชันแบบนี้ในระบบใช้ positional param ทั้งหมดอยู่แล้ว)
  - **`api/save-sidebar-item.php` — validate `iframe_src` เฉพาะจุดนี้จุดเดียว**: `filter_var(..., FILTER_VALIDATE_URL)` + เช็ค scheme ต้องเป็น `http`/`https` เท่านั้น ก่อนเก็บลง DB — ต่างจาก `link_url` (ไม่มี validate เลย แค่เป็นลิงก์ที่คนคลิกเอง) หรือ `image` (`htmlspecialchars()` ครอบทุกจุดที่ print อยู่แล้ว) เพราะ `iframe_src` ถูก render ตรงเป็น `<iframe src="...">` ให้**ผู้ชมทุกคนเห็นทันที** เป็นจุดเสี่ยง XSS จริง ตรงตามหลัก "validate ให้ตรงจุดเสี่ยงจริง" ที่สรุปไว้จากตอนแก้ `featured_image` (ดูหัวข้อ "บั๊ก/ปัญหา" ด้านล่าง) — invalid/ว่าง → เก็บเป็น `null` เฉยๆ ไม่ error ทั้ง request (renderer แค่ไม่ render อะไรออกมา) — ทดสอบยิง `javascript:alert(1)` เข้า `iframe_src` จริง ยืนยันแล้วว่าถูกเก็บเป็น `NULL` ไม่หลุดเข้าหน้าเว็บ
    - **Normalize ข้อมูลตาม type ตอนบันทึก** — `type==='iframe'` บังคับ `content`/`image`/`link_url` เป็นค่าว่าง/null, `type==='article'` บังคับ `iframe_src`/`iframe_height` เป็น `null` กันไม่ให้แถวมีข้อมูลค้างของ type ที่ไม่ได้ใช้ (เช่น สลับ type ไปมาในฟอร์มเดียวกันก่อนกดบันทึก) — กลไก image-cleanup เดิม (`deleteUploadIfUnused()`) ยังทำงานถูกต้องเหมือนเดิมเพราะเทียบแค่ค่า `image` เก่ากับใหม่ ไม่สนว่ามาจาก type ไหน
  - **Renderer-map จริงตามที่ร่างแผนไว้ตั้งแต่ Phase 13** — `partials/sidebar-renderers/article.php` (ย้าย markup เดิมของ `partials/sidebar.php`'s foreach มาทั้งดุ้น ไม่เปลี่ยน) + `sidebar-renderers/iframe.php` (ใหม่ — `<iframe class="sidebar-item-iframe" src=... height=... loading="lazy">`) — `partials/sidebar.php`'s foreach เหลือแค่ `require __DIR__ . '/sidebar-renderers/' . ((...) === 'iframe' ? 'iframe.php' : 'article.php');` ต่อ item หนึ่งบรรทัด — `require` แชร์ scope ของ loop เดิมอยู่แล้วเลย `$item` ใช้ได้ตรงๆ ไม่ต้อง pass เข้าไปเอง (เหมือนกลไกเดิมที่ partial อื่นๆ ในระบบใช้)
  - **`sidebar-item-editor.php` + `assets/editor.js`'s `saveSidebarItem()`** — เพิ่ม `<select id="item-type">` เลียนแบบ `editor.php`'s post/page `<select id="type">` **เป๊ะทุกจุด** (server-side initial `display` จาก state ปัจจุบัน + JS `change` listener toggle `style.display` ตรงๆ ไม่ใช้ CSS class) ห่อฟิลด์เดิม (รูป/ลิงก์/Quill) ไว้ใน `#article-fields`, เพิ่ม `#iframe-fields` (URL + ความสูง) คู่กัน — `saveSidebarItem()` ส่ง `type`/`iframe_src`/`iframe_height` เพิ่มเข้า JSON body
  - **`sidebar-items.php` (หน้าลิสต์แอดมิน)** — เพิ่ม badge "iframe" (reuse `.status-badge.status-scheduled` สีฟ้าที่มีอยู่แล้ว ไม่สร้างสีใหม่) ต่อท้ายชื่อ เฉพาะแถวที่เป็น iframe type เท่านั้น ไม่งั้นแยกไม่ออกว่าแถวไหนเป็นอะไรจากลิสต์เฉยๆ
  - **ของแถมนอกแผน: หน้า embed เปล่าสำหรับใช้เป็น `iframe_src`** — `youtube-embed.php` (วิดีโอ YouTube ฝัง video ID ไว้ในโค้ดตัวแปร `$videoId` ตัวเดียว แก้ง่าย, full-bleed ไม่มีขอบ/margin) และ `feed-embed.php` (ฟีดข่าวสั้นล้วนๆ ไม่มี header/nav/footer ของเว็บเลย) — ทั้งสองไฟล์**ไม่ผ่าน `render_header()`/`render_sidebar()`/`render_footer()` เลยโดยตั้งใจ** เพราะจะไปโหลด chrome ทั้งเว็บเข้ามาซ้อนในกล่อง iframe เล็กๆ ที่ไม่ต้องการ — `feed-embed.php` reuse `api/feed-poll.php`/`assets/feed.js` เดิมได้ตรงๆ 100% ไม่ต้องแก้อะไรเลยเพราะทั้งคู่ผูกอยู่กับ `#feed-list` element เท่านั้นมาตั้งแต่ต้น ไม่เคยผูกกับ chrome ของหน้า
    - **`assets/feed.css` ปรับ font-size แยกต่อหน้าได้แล้ว** — `.feed-item-content`/`.feed-item-time` เปลี่ยนจาก `font-size: 18px`/`13px` ตายตัว เป็น `var(--feed-item-font-size, 18px)`/`var(--feed-item-time-font-size, 13px)` (ค่า default เท่าเดิมเป๊ะ `feed.php` หน้าจริงไม่กระทบเลย) แล้ว `feed-embed.php` override 2 ตัวแปรนี้เป็นค่าเล็กลง (14px/11px) ผ่าน `<style>` ของตัวเองเท่านั้น — ตัวอย่างการใช้ pattern "CSS custom property มีค่า default + override เฉพาะจุดผ่าน scope เสริม" (แบบเดียวกับ `--sidebar-width`, ดู "หลักการรอง" ต้นเอกสาร) ไม่สร้าง CSS ไฟล์คู่ขนานใหม่
  - **ทดสอบยืนยันด้วยของจริงทั้งหมด**: สร้าง iframe item ผ่านแอดมินจริง (YouTube URL) → render เป็น `<iframe>` จริงบนหน้าเว็บสาธารณะ (เห็นวิดีโอเล่นได้จริง) — แก้ไข article item เดิมที่มีอยู่ก่อนหน้าฟีเจอร์นี้ ยืนยัน field เดิมรอบทริปกลับมาครบไม่มีอะไรหาย — ลอง `javascript:` URL ถูกบล็อก — เช็ค `feed-embed.php`/`youtube-embed.php` ทั้ง render เดี่ยวๆ และ nested อยู่ใน sidebar iframe จริง ผ่านหมด

- **บั๊ก/ปัญหาที่เจอและแก้ไปแล้ว** (ไว้เป็น reference ถ้าเจอซ้ำ)
  - สิทธิ์เขียนไฟล์ `uploads/`, `articles/` (Apache รันเป็น user `daemon` ต่างจากเจ้าของโฟลเดอร์)
  - **Bullet/numbered list ต้องมีทั้ง `quill.snow.css` โหลดอยู่ และ class `.ql-editor` บน wrapper ด้วย ไม่งั้น marker ไม่ขึ้นเลย** (ไม่ใช่แค่ "ไม่งั้นแสดงเป็นเลขหมด" อย่างที่เข้าใจตอนแรก) — ตอนแรกแก้ปัญหาด้วยการโหลด `quill.snow.css` เฉพาะหน้า `article.php` ก็พอ แต่พอทำ sidebar item ที่ render เนื้อหาแบบเดียวกันในหน้าอื่นๆ (ไม่มีทั้ง `quill.snow.css` และ class `.ql-editor`) ถึงเจอว่า marker หายไปเงียบๆ ทุกที่ที่ไม่ใช่ 4 หน้านั้น — แก้ถาวรด้วยการย้าย `quill.snow.css` ไปโหลดทุกหน้าใน `partials/header.php` แทน (ดู "เปิด/ปิด sidebar แบบ 2 ชั้น"/"รวม CSS ของเนื้อหาที่มาจาก Quill" ด้านบน)
  - Auto-link คำนวณตำแหน่ง cursor ผิดถ้าใช้ `getSelection()` (ไทม์มิ่งไม่นิ่ง) → แก้เป็นคำนวณจาก delta ของ Quill โดยตรง
  - **Quill จะลบ DOM element ที่มันไม่รู้จักทิ้งอัตโนมัติ** (เช่น `<span class="mblog-caption">` ที่แทรกตรงๆ) → แก้โดยเก็บ caption เป็น `data-caption` attribute บน `<img>` ระหว่างแก้ไข แล้วค่อยแปลงเป็น `<span>` จริงตอนบันทึก/โหลดกลับมาแก้ไข (หลักการนี้ใช้ได้กับ custom UI อื่นๆ ที่จะเพิ่มในอนาคตด้วย)
  - CSS ของ Quill เองมี specificity สูงกว่า custom CSS ธรรมดา (เช่น code block พื้นหลังเข้ม) ต้องใช้ `!important` ทับ
  - highlight.js จาก jsDelivr path `lib/common.min.js` เป็นไฟล์สำหรับ bundler (`require()`) ใช้ตรงกับ `<script>` ไม่ได้ ต้องใช้ cdnjs build ที่ browser-ready
  - Quill แอบใส่ `<select>` เลือกภาษาโปรแกรม (UI ตอนแก้ไข) ติดไปกับเนื้อหาที่บันทึก ต้องซ่อน/ล้างออกตอนแสดงผลจริง
  - ไม่มี `<meta name="viewport">` เลยทั้ง 3 หน้า (บั๊กร้ายแรงสุดสำหรับ mobile — ถ้าไม่มี CSS responsive จะไม่มีผลอะไรเลย)
  - **timezone ไม่ตรงกันระหว่าง PHP ที่รัน migration script (Homebrew CLI, default `UTC`) กับ PHP ของ XAMPP/Apache (`php.ini` ตั้ง `Europe/Berlin`)** — เวลาที่เขียนลง MySQL ถูกอ่านกลับมาคลาดเคลื่อนหลายชั่วโมงเพราะแต่ละฝั่งตีความ string เวลาเดียวกันเป็นคนละ timezone → แก้ด้วยการบังคับ `date_default_timezone_set()` ตายตัวใน `config.php` (อ่านค่าจาก `config/settings.php`) ไม่พึ่ง `php.ini` ของเครื่อง/โปรเซสที่รันเลย — ถ้าเปลี่ยน timezone ของเว็บ (เช่น UTC → Asia/Bangkok) ต้องแปลงข้อมูลเก่าที่เก็บไว้ด้วย (shift ชั่วโมงให้ตรง) ไม่งั้นข้อมูลเก่าจะตีความผิดทันที
  - **Unicode combining marks (สระ/วรรณยุกต์) อยู่ในหมวด Mark (`\p{M}`) ไม่ใช่ Letter (`\p{L}`)** — regex sanitize ชื่อไฟล์/slug ที่อนุญาตแค่ `\p{L}\p{N}` จะตัดสระ/วรรณยุกต์ไทยทิ้งกลางคำ (เช่น "รูปภาพ" → "ร-ปภาพ") ต้องอนุญาต `\p{M}` เพิ่มด้วยเสมอเวลาทำ sanitize ที่ต้องรองรับภาษาไทย/สคริปต์ที่มี combining marks (จีน/ญี่ปุ่น/อาหรับ/ฮินดี/เวียดนามก็มีเช่นกัน)
  - **จำกัดความยาวชื่อไฟล์ด้วย "จำนวนตัวอักษร" ไม่ปลอดภัยสำหรับภาษาที่ใช้หลายไบต์ต่อตัวอักษร** (ไทย/จีน/ญี่ปุ่น/ฮินดี ใช้ 3 ไบต์/ตัวอักษรใน UTF-8) — จำกัดที่ 100 ตัวอักษรทำให้ได้ชื่อไฟล์ยาวถึง 300 ไบต์ ซึ่งเกินขีดจำกัดทั่วไปของระบบไฟล์ (~255 ไบต์) บน macOS/APFS ไม่มีปัญหา (นับความยาวแบบ UTF-16 code unit) แต่ **Linux (ext4/XFS ซึ่งเป็น production hosting ส่วนใหญ่) จะเซฟไฟล์ไม่ได้เงียบๆ** — ต้องจำกัดด้วย "จำนวนไบต์" (`strlen()`) ไม่ใช่ "จำนวนตัวอักษร" (`mb_strlen()`) เวลา cap ความยาว path/filename
  - **regex ตรวจสอบ `featured_image` ค้างจาก schema เก่า พอเปลี่ยนโครงสร้างโฟลเดอร์อัปโหลดแล้วลืมอัปเดตตาม** — เดิมรองรับแค่ `uploads/FILENAME.ext` (flat, อักษรละตินเท่านั้น) พอเปลี่ยนเป็น `uploads/YYYY/MM/` + ชื่อไฟล์ภาษาไทย/จีน (WP-style) ทำให้ featured image ที่เลือกไว้ถูกตัด/ปฏิเสธเงียบๆ ทุกครั้งโดยไม่มี error ใดๆ — บทเรียน: ทุกจุดที่ validate "รูปร่างข้อมูล" (path, format) ต้องอัปเดตตามทุกครั้งที่โครงสร้างข้อมูลนั้นเปลี่ยน ไม่งั้นจะเงียบและตรวจไม่เจอจนกว่าจะมีคนสังเกตผลลัพธ์ผิดปกติ
  - **เบราว์เซอร์ cache ไฟล์ CSS/JS เก่าค้างหลังแก้ไข** — แก้ `assets/*.css`/`*.js` แล้วเบราว์เซอร์ยังโหลดไฟล์เก่าอยู่ (สังเกตผ่าน `document.styleSheets[].cssRules.length` เทียบกับไฟล์จริง) → แก้ด้วยการเติม cache-busting query string ต่อท้ายทุก asset ใน `partials/header.php` (`?v=` + `filemtime()` ของไฟล์นั้นๆ) เปลี่ยนไฟล์เมื่อไหร่ query string เปลี่ยนตาม บังคับโหลดใหม่ทันที — ปัญหานี้จะเกิดกับผู้ใช้จริงตอน deploy ด้วย ไม่ใช่แค่ตอน dev
  - **ข้อความไทยยาวๆ ไม่มีช่องว่างเลย ล้นกรอบที่ความกว้างตายตัวแทนที่จะขึ้นบรรทัดใหม่** — เจอตอนทำ dropdown เมนู: "นโยบายความเป็นส่วนตัว" ไม่มีจุดให้ browser ตัดบรรทัดตามปกติ (wrap ที่ space) เพราะภาษาไทยไม่มี space คั่นคำ ต้องใส่ `overflow-wrap: break-word` ในทุก container ที่ความกว้างตายตัวและอาจรับข้อความไทย/CJK ยาวๆ ไม่งั้นข้อความจะทะลุกรอบแทนการ wrap
  - **Dropdown เมนูวางตำแหน่งแบบ `left:0` เสี่ยงล้นขอบจอมือถือถ้าปุ่มอยู่ใกล้ขอบขวา** — แก้ไปมาหลายรอบระหว่างทำ desktop dropdown + mobile accordion คู่กัน สุดท้ายได้ข้อสรุป: ถ้า component เดียวกันต้องรองรับทั้ง 2 breakpoint ให้คิดเรื่อง anchor position ตามบริบทจริงของแต่ละจอ ไม่ใช้ค่าเดียวข้ามทั้งสองจอ — ทางแก้จริงที่ใช้คือแยก UI มือถือ/เดสก์ท็อปออกจากกันไปเลย ทำให้ desktop กลับไปใช้ `left:0` (สวยกว่า ไม่ล้นเพราะพื้นที่กว้างพอเสมอ) ได้อย่างอิสระโดยไม่ต้องกังวลเรื่องมือถือเลย เพราะมือถือไม่ใช้ component นี้อีกต่อไป
  - **`margin` ระหว่าง toggle กับ dropdown ทำให้ CSS `:hover` หลุดกลางทาง** — ตอนเปลี่ยน dropdown เมนูจาก "คลิกเปิด" เป็น "hover เปิด" (แบบ WP) เจอบั๊ก: มี gap 10px ระหว่างปุ่มเมนูกับกล่องเมนูย่อย (ทำด้วย `margin-top`) พอเลื่อนเมาส์จากปุ่มลงไปยังเมนูย่อย เคอร์เซอร์ผ่านช่องว่างที่ไม่ได้อยู่ใน DOM ของทั้งปุ่มหรือกล่อง ทำให้ `:hover` หลุดและเมนูปิดก่อนจะเลื่อนไปถึง — แก้โดยเปลี่ยนจาก `margin-top` (ช่องว่างจริง นอกกล่อง ไม่นับเป็นพื้นที่ hover) เป็น `padding-top` (ช่องว่างภายในกล่อง นับเป็นพื้นที่ hover ต่อเนื่อง) หลักการทั่วไป: dropdown แบบ hover ต้องไม่มี "ช่องว่างที่ไม่มีใครเป็นเจ้าของ" คั่นระหว่าง trigger กับเนื้อหา
  - **`updateSiteSettings()` (`includes/settings.php`) เขียนลง DB แต่ไม่เคย sync static cache ของ `getSettings()`** — เจอตอนทดสอบ `pinArticle()`/`toggleStickyArticle()` (ก่อนจะออกแบบใหม่เป็น `setStickyArticles()`) ด้วยสคริปต์ทดสอบที่ยิงซ้ำในคำขอเดียวกัน: เขียนค่าใหม่ผ่าน `updateSiteSettings()` แล้วอ่านกลับทันทีด้วย `siteSetting()`/`getStickyArticleIds()` ได้ค่าเก่า — สาเหตุคือ `getSettings()` cache ผลลัพธ์ไว้ใน `static $settings` ตัวแปรเดียวตลอด request แต่ `updateSiteSettings()` ไม่เคยแตะ cache นี้เลยหลังเขียน DB — ไม่เคยมีใครเจอมาก่อนเพราะทุกจุดที่เรียก `updateSiteSettings()` (เช่น `settings.php`) มี `header('Location: ...'); exit;` ตามทันทีเสมอ (request ใหม่ = cache ใหม่ที่อ่านถูกต้อง) จนกว่าจะมีจุดที่ไม่ redirect ทันทีแบบนี้ — แก้ด้วยให้ `getSettings(?array $overrides = null)` รับ override เสริม (merge เข้า cache ตรงๆ ไม่ query ใหม่) แล้วให้ `updateSiteSettings()` เรียก `getSettings($values)` ท้ายฟังก์ชันเสมอ
  - **MySQL มีนาฬิกาของตัวเอง (`NOW()`) แยกจาก PHP โดยสิ้นเชิง — คนละเรื่องกับบั๊ก timezone ของ PHP CLI vs Apache ด้านบน** — `NOW()` ของ MySQL ผูกกับ `@@session.time_zone` ซึ่ง default เป็น `SYSTEM` (ตาม timezone ของเครื่อง OS ที่รัน MySQL) ไม่เกี่ยวกับ `date_default_timezone_set()` ของ PHP หรือ `mblog_settings.timezone` ที่เว็บตั้งไว้เลย — ตอนทำ scheduled publishing (`publicVisibilitySql()` เทียบ `published_at <= NOW()` ที่ SQL โดยตรง) พบว่าถ้า MySQL อยู่คนละ timezone กับที่เว็บตั้งไว้ (เช่น MySQL = UTC, เว็บตั้ง Bangkok = UTC+7) บทความที่ตั้งเวลาไว้ "9 โมงเช้า" จะไม่โผล่จนถึง 16:00 น. จริง (ช้าไป 7 ชั่วโมง) — บนเครื่อง dev ไม่มีอาการให้เห็นเพราะ timezone ของเครื่องบังเอิญตรงกับที่เว็บตั้งไว้อยู่แล้ว (เช็คแล้วด้วย `SELECT @@session.time_zone` vs `SELECT UTC_TIMESTAMP()` ก่อนแก้ เพื่อยืนยันว่าไม่ใช่แค่ทฤษฎี) — แก้ด้วย `db()->exec('SET time_zone = ' . db()->quote(date('P')));` ใน `config.php` **หลัง** `date_default_timezone_set()` เท่านั้น (ต้องมาหลัง เพราะ `db()` ถูกเรียกครั้งแรกไปแล้วตอน `siteSetting('timezone', ...)` ดึงค่ามาตั้ง PHP timezone เอง — ถ้าเรียก `SET time_zone` ไว้ใน `db()` ตอน connect เลยจะได้ค่า offset ผิดเพราะ PHP timezone ยังไม่ถูกตั้งตอนนั้น) ใช้ offset ตัวเลข (`+07:00`) ไม่ใช่ชื่อโซน (`Asia/Bangkok`) เพราะ MySQL ต้องมีตาราง `mysql.time_zone_name` โหลดไว้ก่อนถึงจะรู้จักชื่อโซน ซึ่งเครื่องส่วนใหญ่ (รวม XAMPP) ไม่ได้โหลดไว้ default

- **งานปรับปรุงย่อยรอบ 2026-08-06 (หลายจุด ไม่ผูก Phase เดียว — เน้นระบบธีมมืด + งานเก็บกวาด UI แอดมิน)**
  - **โทนพื้นหลัง (ธีมมืด) เลือกได้ 5 แบบแล้ว ไม่ใช่แค่ดำอย่างเดียว** — `theme-editor.php`: dropdown ใหม่ `#s-dark-tone` (ดำ/น้ำเงิน/เขียว/แดง/ม่วง) คู่กับ `#s-tone` เดิม (โทนพื้นหลังธีมสว่าง) — `DARK_TONE_PRESETS` (object คงที่ฝั่ง JS) กำหนด hue+saturation ต่อโทนให้ `deriveFromSimple()`: **"ดำ"** คงสูตรเดิมเป๊ะ (hue สืบทอดจากโทนสว่างที่เลือก, saturation ล็อกต่ำ ~6-10% แทบมองไม่ออก) ส่วน**น้ำเงิน/เขียว/แดง/ม่วง**บังคับ hue ของตัวเอง (230/150/355/275) แล้วดัน saturation เฉพาะโทนพื้นหลัง (canvas/card/secondary/hairline เป็น ~20-32%) ขึ้นให้เห็นชัดแบบ dark-mode ที่มีสีสัน (อ้างอิงตัวอย่าง glyphsandgrids.com/sevengrid.app) — **ตัวอักษร (ink/body/muted) ยังคง desaturated ต่ำเหมือนเดิมทุกโทน** เพราะ contrast มาจาก lightness ไม่ใช่ hue/saturation ปรับแค่ "บรรยากาศ" ไม่กระทบความอ่านง่าย — เลือก hue 355 (ไวน์) แทน 0 (แดงล้วน) ให้สีแดงเพราะ hue 0 ชนกับ `--color-error` ที่มีอยู่แล้วในเว็บ จะดูเหมือนมี error ค้างตลอดเวลา
  - **เลือกโทนสว่าง/โทนมืดให้อัตโนมัติตรงกับค่าที่บันทึกไว้แล้ว (บั๊กที่เจอและแก้แล้ว)** — เดิม `#s-tone` เปิดหน้ามาโชว์ตัวเลือกแรก ("ครีมอุ่น") เสมอไม่ว่าจะเคยบันทึกโทนไหนไว้จริง เพราะ `<select>` ไม่มี `selected` ผูกกับค่าที่โหลดมาเลย — แก้ด้วยการวน `<option>` ทุกตัวตอนโหลดหน้า คำนวณ hex จริงของแต่ละตัวเลือกด้วย `hslToHex()` ที่มีอยู่แล้ว เทียบกับ `current.light.canvas`/`current.dark.canvas` ตัวไหนตรง (string เท่ากันเป๊ะ) ก็ตั้ง `.value` ให้ตรงนั้น — ถ้าไม่ตรงกับ preset ไหนเลย (เช่นเคยแก้สี canvas เองในโหมดขั้นสูง) ปล่อยเป็นค่า default ของ browser ไป ไม่ error
  - **`color-reference.php`: เพิ่มปุ่มสลับ "ค่าที่ใช้ตอนนี้ / ค่าเริ่มต้น" (ทำแล้ว)** — เดิมหน้านี้โชว์แต่ hex ที่ hardcode คัดลอกมาจาก `assets/base.css` เท่านั้น (ตามคอมเมนต์ต้นไฟล์: ตั้งใจไม่ใช้ `var(--token)` จริงเพราะต้องโชว์ light+dark คู่กัน) — ถ้าเว็บเคยปรับแต่งสีผ่าน `theme-editor.php` แล้ว หน้านี้จะโชว์ค่าเก่าที่ไม่ตรงกับเว็บจริงอีกต่อไป เข้าใจผิดได้ — เพิ่ม `withCurrentValues()` แปลง palette เดิมให้ดึงจาก `getThemeColors()` แทนเมื่อกด "ค่าที่ใช้ตอนนี้" (ค่า default ยังคงเดิมทุกจุดเมื่อกด "ค่าเริ่มต้น") ใช้ query string `?view=current`/`?view=default` ล้วนๆ ไม่มี JS เลย (bookmark/แชร์ลิงก์ตรงโหมดที่ดูอยู่ได้) — ค่า default ของหน้า (ไม่มี `?view=`) ตั้งเป็น "ค่าที่ใช้ตอนนี้" เพราะเป็นสิ่งที่อยากรู้บ่อยกว่า — โชว์ข้อความเตือนถ้ายังไม่เคยปรับแต่งเลย (`hasCustomThemeColors()` false) ว่าค่าที่ใช้ตอนนี้เหมือนค่าเริ่มต้นทุกจุด กันสับสนว่าทำไมสองโหมดดูเหมือนกัน
  - **`theme-preview.php` เอาออกจากเมนู sidebar แอดมิน ย้ายไปเป็นลิงก์ในหน้า `theme-editor.php` แทน** (`adminNavGroups()` ตัด entry นี้ทิ้ง, เพิ่มลิงก์ "ดูตัวอย่างองค์ประกอบทั้งหมด →" ท้ายฟอร์ม `theme-editor.php`) — ใช้น้อยกว่าเมนูอื่นในกลุ่ม "ตั้งค่าเว็บ" มาก และมีความเกี่ยวข้องกับ `theme-editor.php` โดยตรงอยู่แล้ว (พรีวิวสีที่เพิ่งปรับ) เข้าถึงจากตรงนั้นสมเหตุสมผลกว่าเมนูแยก
  - **`feed-embed.php` ไม่รับชุดสี custom ที่ปรับผ่าน `theme-editor.php` เลย (บั๊กที่เจอและแก้แล้ว)** — หน้านี้เป็น "bare-bones" ตั้งใจไม่ `include partials/header.php` (กัน chrome เต็มเว็บมาซ้อนใน iframe เล็กๆ) แต่เลยพลาด `renderThemeColorStyle()` (`includes/theme-colors.php`) ที่ปกติ header.php echo ให้ทุกหน้าไปด้วยโดยไม่ตั้งใจ — เห็นแต่สีดีฟอลต์ของ `assets/base.css` เสมอ ทั้งที่เว็บจริงปรับสีอื่นไปแล้ว (สังเกตจากแถบหัว "ฟีดข่าว" ในหน้าสาธารณะยังเป็นสีส้มดีฟอลต์ ทั้งที่ปุ่ม/เมนูอื่นทั้งเว็บเป็นสีน้ำเงินที่ปรับไว้) — แก้ด้วยการ `require_once` + echo `renderThemeColorStyle()` เองในหน้านี้ด้วย ตำแหน่งเดียวกับที่ header.php ทำ (หลัง `assets/feed.css`)
    - **บั๊กที่เจอระหว่างแก้ (ซ้อนอยู่ในบั๊กบน)**: `require_once __DIR__ . '/includes/theme-colors.php';` ใหม่ใน `feed-embed.php` ชนกับ `require` (ไม่ใช่ `require_once`) เดิมที่อาจโหลดไฟล์เดียวกันซ้ำผ่านทาง include อื่น — ทั่วไฟล์ในโปรเจกต์นี้บางจุดใช้ `require` เปล่าๆ ผสมกับ `require_once` ปนกัน ถ้ามีจุดไหนที่ไฟล์เดียวกันถูกโหลดทั้ง 2 แบบ (ครั้งแรกผ่าน `require_once` ที่อื่น ครั้งสองผ่าน `require` เปล่าๆ) จะ fatal error "cannot redeclare function" ทันที — เจอเคสจริงคล้ายกันตอนเพิ่ม badge นับไฟล์กำพร้าด้านล่างนี้เช่นกัน (ดูหัวข้อถัดไป) เป็นรูปแบบบั๊กที่ควรระวังซ้ำเวลาเพิ่ม require ใหม่เข้าไฟล์ที่มีจุด include เดิมอยู่แล้วหลายทาง
  - **เมนู "ไฟล์กำพร้า" มีตัวเลขนับจำนวนไฟล์ในป้าย badge แล้ว** (`adminNavGroups()`: `'badge' => count(scanOrphanUploads())`) เหมือนเมนูอื่นๆ ในกลุ่ม — หนักกว่า badge อื่นเล็กน้อยเพราะ `scanOrphanUploads()` ไล่สแกนทั้งโฟลเดอร์ `uploads/` + query DB 2 ครั้งต่อไฟล์ (`uploadPathInUse()`) ไม่ใช่แค่ `COUNT(*)` เดียวเหมือนตัวอื่น แต่ที่ขนาดเว็บปัจจุบัน (ไฟล์หลักสิบ) ยังไม่มีผลกระทบที่วัดได้ — ต้องแก้ `orphan-files.php` เองด้วย (เปลี่ยน `require` เป็น `require_once` สำหรับ `includes/orphan-files.php`) กันชนกับ `require_once` ตัวใหม่ใน `admin-nav.php` แบบเดียวกับบั๊ก `feed-embed.php` ด้านบน — สังเกตแพทเทิร์นนี้ได้ว่าเกิดซ้ำ: **เพิ่ม `require_once` ใหม่ในไฟล์ที่ใครก็ตามเรียกด้วย `require` เปล่าๆ อยู่แล้ว ต้องเช็ค/แก้จุดนั้นให้เป็น `require_once` คู่กันเสมอ**
  - **ปักหมุดมีตัวบ่งชี้ให้เห็นในหน้าสาธารณะแล้ว** — เดิม `manage-articles.php` (แอดมิน) มี 📌 ต่อท้ายชื่อบทความที่ปักหมุดอยู่แล้ว (ผ่าน `isStickyArticle()`) แต่ 5 หน้ารายการสาธารณะที่ใช้ `partials/article-list.php` ร่วมกัน (`articles.php`/`pages.php`/`category.php`/`tag.php`/`drafts.php`) ไม่มีเลย — เพิ่ม badge `<span class="status-badge status-pinned" title="ปักหมุด">📌</span>` (ไอคอนล้วน ไม่มีคำว่า "ปักหมุด" ต่อ ตามที่ขอ) หน้าชื่อเรื่อง มีผลครบทั้ง 5 หน้าพร้อมกันเพราะใช้ partial กลาง
  - **`articles.php`: เอาหัวข้อ `<h1>บทความทั้งหมด</h1>` ออก เฉพาะหน้านี้หน้าเดียว** — หน้าอื่นที่ใช้ partial เดียวกัน (`category.php`/`tag.php` ฯลฯ) ยังมีหัวข้อของตัวเองตามปกติเพราะบอกความเฉพาะของหน้านั้นๆ ต่างจาก `articles.php` ที่ไม่จำเป็น — `$pageTitle` (ใช้แค่ตั้งชื่อแท็บเบราว์เซอร์) ไม่ได้ตัดออก แยกจาก `<h1>` ที่เป็น UI คนละส่วนกัน
  - **`manage-feed.php`: เพิ่ม checkbox + bulk action (ลบเป็นชุด) และ pagination (60 รายการ/หน้า)** — UI ตามแพทเทิร์นเดียวกับ `includes/manage-list.php` (checkbox แถวผูก `form="bulk-form"` ข้ามฟอร์ม กัน `<form>` ซ้อน `<form>` แบบที่เคยแก้ไปแล้วในนั้น) แต่ฟีดข่าวไม่มีสถานะ publish/draft/trash แบบบทความ (ดู schema `mblog_feed_items`) เลย dropdown "การดำเนินการเป็นชุด" เหลือตัวเลือกเดียวคือ "ลบ" — เพิ่ม `bulkDeleteFeedItems()` (`includes/feed.php`) และ `getFeedItemsForAdmin($page, $perPage)` แยกจาก `getFeedItems($limit)` เดิมโดยตั้งใจ (ตัวเดิมยังเป็น "ล่าสุด N รายการ ไม่แบ่งหน้า" ให้ `feed.php`/`feed-embed.php`/`api/feed-poll.php` ใช้เหมือนเดิม ไม่กระทบ) — `assets/manage-list.js` เพิ่ม case `'delete'` ใน confirm dialog ของ bulk action (เดิมมีแค่ `trash`/`permanently_delete`)
  - **ตาราง `manage-feed.php`/`sticky-items.php` ล้นขอบ card จน scroll แนวนอน (บั๊กที่เจอและแก้แล้ว, สาเหตุไม่ตรงไปตรงมา)** — ตอนแรกคิดว่าเป็นเพราะคอลัมน์ข้อความ/ชื่อยาวเกิน แก้ด้วย `table-layout: fixed` + กำหนดความกว้างคอลัมน์อื่นตายตัวปล่อยคอลัมน์เนื้อหารับพื้นที่ที่เหลือ (คลาสใหม่ `.feed-table`/`.sticky-search-table`/`.sticky-pinned-table` ใน `components.css` แยกจาก `.admin-table` เดิมที่หน้าอื่นยังใช้ nowrap+scroll ตามปกติ) — ยังล้นอยู่หลังแก้รอบแรก เช็คจริงด้วย `scrollWidth` ต่อเซลล์ถึงเจอสาเหตุจริง: **`relativeTimeTag()`'s hover-tooltip pseudo-element (`[data-tooltip]::after`) กว้างกว่าคอลัมน์ "เวลา" มาก แม้จะมองไม่เห็น (opacity:0) ก็ยังถูกนับเข้า `scrollWidth` ของ parent** ดันตารางล้นออกไปโดยที่ตาไม่เห็นสาเหตุเลย — แก้ทับด้วย `overflow-x: hidden` เฉพาะ wrapper ของตารางเหล่านี้ (`.feed-table-scroll`) เพราะไม่มีคอนเทนต์ที่มองเห็นได้จริงต้องพึ่ง scroll อยู่แล้วหลังกำหนดความกว้างคอลัมน์ตายตัว
  - **`admin-table td input[type=number]` ไม่มีสไตล์ของเว็บ (บั๊กที่เจอและแก้แล้ว)** — ช่องกรอก "ลำดับ" ใน `sticky-items.php`/`sidebar-items.php` (`<input type="number" ... style="max-width:80px;">` วางเดี่ยวๆ ใน `<td>` ไม่ได้ห่อด้วย `.field` เหมือน input อื่นทั้งเว็บ) เลยไม่โดน `.field input[type=number]` (มี border/padding/background ของธีม) ตกไปใช้สไตล์ default ของ browser เฉยๆ — แก้ด้วย rule ใหม่ `.admin-table td input[type=number]` (ไม่ต้องแก้ HTML เลยสักจุด มีผลทั้ง 2 หน้าที่ใช้ pattern เดียวกันพร้อมกัน)
  - **`.field select`/`.te-field select`: default เปลี่ยนจาก `width: 100%` เป็น `width: auto`** — เดิม dropdown สั้นๆ (เช่น "ขวา"/"ซ้าย" ใน `settings.php`, โทนพื้นหลังใน `theme-editor.php`) กว้างเท่า input เต็มบรรทัดเสมอทั้งที่ตัวเลือกสั้นกว่ามาก — เปลี่ยนเป็นกว้างพอดีเนื้อหา (browser native sizing จากตัวเลือกที่ยาวสุด) ตัวไหนอยากได้เต็มความกว้างจริงๆ (เช่น `#pv-select` ใน `theme-editor.php`) กำหนด `width:100%` เฉพาะจุดเอง — `.field` คง `max-width: 260px` ไว้กันกรณี option ข้อความยาวเกิน

---

- **ระบบล็อกอิน/สิทธิ์ (Phase 3) + โปรไฟล์ทีมงาน/topbar ทั้งเว็บ (Phase 15, นอกแผนเดิม, ทำเสร็จ 2026-08-06)**
  - **Capability-based ทั้งระบบ ไม่เช็คชื่อ role ตรงๆ** — `includes/auth.php`: `ROLE_CAPABILITIES` (array คงที่ เดียวจุดเดียวกำหนด access matrix ทั้งเว็บ) `admin` → `['*']`, `editor` → `edit_articles`/`publish_articles`/`edit_others_articles`/`delete_articles`/`manage_categories`, `author` → `edit_articles`/`publish_articles` เท่านั้น (ไม่มี `edit_others_articles` — ใครแก้ของคนอื่นไม่ได้) — `userCan($capability)` เช็คกับ array นี้, `requireLogin()`/`requireCapability($capability)` (หน้าเว็บ, redirect ไป `login.php` หรือ render หน้า 403) / `requireApiLogin()`/`requireApiCapability($capability)` (API, ตอบ JSON 401/403 แทน redirect) เป็น gate เดียวที่ทุกหน้า/endpoint ป้องกันเรียกที่บรรทัดแรก — grep ทั้งโปรเจกต์แล้วมีข้อยกเว้นแค่ 2 จุดที่เช็ค `$user['role'] === 'admin'` ตรงๆ (มีคอมเมนต์อธิบายในโค้ดทั้งคู่ว่าทำไม): `users.php` (กันลบ admin คนสุดท้าย) และ `profile.php` (กันลดสิทธิ์ตัวเองออกจาก admin) — เหตุผลคือคำถามแบบ "ทำแล้วในระบบจะไม่เหลือใครมีสิทธิ์ admin เลยไหม" ไม่ใช่ "user นี้ทำ X ได้ไหม" เลยไม่มีทางแสดงผ่าน `userCan()` ได้ — `articleOwnerFilter()` เป็นข้อยกเว้นอีกแบบ (ไม่ใช่ yes/no capability แต่เป็นตัวกรองระดับ query สำหรับ author เห็นแค่ของตัวเอง) ใช้จริงใน `includes/manage-list.php` (ครอบ `manage-articles.php`/`manage-pages.php`) — **`drafts.php` ยังไม่ใช้ตัวกรองนี้** (ล็อกอินก่อนเข้าแล้วแต่เห็นร่างของทุกคนปนกันหมด — งานค้าง)
  - **`login.php`: เข้าด้วยอีเมลหรือ username ก็ได้** (นอกแผนเดิมที่ระบุแค่ email/password) — query เดียว `WHERE email = ? OR username = ?` ทั้งสองพารามิเตอร์ผูกกับค่าเดียวกันจาก input ชื่อ `identifier` — error message เดียวกันไม่ว่าจะพิมพ์ผิดอีเมล/username/รหัสผ่าน (กัน enumerate บัญชีที่มีจริง) — `session_regenerate_id(true)` ตอน login สำเร็จ (กัน session fixation) — `redirect` query param validate ด้วย regex จำกัดแค่ path relative ลงท้าย `.php` เท่านั้น (กัน open redirect)
  - **สลับชื่อตาราง `mblog_users`↔`mblog_staff`, `mblog_readers`↔`mblog_users`** — ตอนแรกตั้งชื่อ `mblog_users` ให้ทีมงานเว็บ (Phase 3 เดิม), `mblog_readers` ไว้เผื่อผู้ชมทั่วไปในอนาคต ภายหลังเห็นว่าชื่อสื่อความหมายผิดทาง (เว็บทั่วไป "users" มักหมายถึงผู้ชมวงกว้าง ไม่ใช่ทีมงาน) — แก้ด้วย `RENAME TABLE mblog_users TO mblog_staff, mblog_readers TO mblog_users` ประโยคเดียว (atomic, ไม่มีช่วงชนชื่อระหว่างทาง) FK `fk_mblog_articles_author` ตามไปเองอัตโนมัติเพราะ InnoDB ผูก FK ด้วย internal table id ไม่ใช่ชื่อ — backup DB ก่อนรันเสมอ (`mysqldump`) — migrate โค้ดตามทุกจุดที่มีชื่อตารางในนั้น (query string ทั้งใน `.php`/`.sql`) รวม 7 ไฟล์ (ยืนยันด้วย `grep` ก่อน-หลังว่าไม่เหลือชื่อเก่า) — **ตั้งใจไม่ rename ไฟล์ `.php`/`.sql` ตาม** (เช่น `includes/users.php` ยังชื่อเดิม ทั้งที่ตอนนี้เป็น data layer ของ `mblog_staff`) ลดความเสี่ยง/จุดที่ต้องแก้ตาม (require/relative path หลายจุด) — คอมเมนต์ในไฟล์อธิบายไว้ว่าทำไมชื่อไฟล์กับชื่อตารางไม่ตรงกัน
  - **โปรไฟล์เต็มรูปแบบ** — เพิ่มคอลัมน์ `username`/`first_name`/`last_name`/`phone`/`line_id`/`avatar_path` ให้ `mblog_staff` (`database/phase4_staff_profile.sql`) — `username` ต้อง backfill จาก local-part ของอีเมลก่อน (`SUBSTRING_INDEX(email, '@', 1)`) เพราะมีแถวเก่าอยู่แล้ว ทำ `NOT NULL UNIQUE` ทีหลังไม่ได้ในประโยคเดียว (ต่างจาก `mblog_users` ที่ว่างเปล่า ทำ `NOT NULL` ตั้งแต่แรกได้เลย) — mirror schema เดียวกันไปที่ `mblog_users` ไว้ล่วงหน้าด้วย แม้ยังไม่มีหน้าเว็บฝั่งผู้ชมใช้งานจริง (ตัดสินใจร่วมกันว่าทำเผื่อไว้ตอนนี้เลย ไม่ต้อง migrate ซ้ำทีหลัง)
  - **`generateUsernameFromEmail()`** (`includes/users.php`) — สร้าง username จาก local-part อีเมล (lowercase, ตัดอักขระที่ไม่ใช่ `a-z0-9_.-` ทิ้ง) ชนกันต่อท้าย `-2`/`-3`/... จนกว่าจะไม่ซ้ำ (WP-style เหมือน `sanitizeUploadFilename()` ใน `api/upload.php`) — ใช้ตอนสมัครผ่าน `users.php`/`setup.php`/`scripts/create-admin.php` (ไม่ระบุ username เอง) เพื่อให้ฟอร์ม "เพิ่มผู้ใช้ใหม่" เหลือแค่อีเมล+รหัสผ่าน (ตามที่ตัดสินใจ — ไม่ต้องกรอก username/role/โปรไฟล์ตั้งแต่แรก) — role default เป็น `author` (สิทธิ์ต่ำสุด จนกว่า admin จะเลื่อนขั้นเอง) — แก้ username ที่ auto-gen ได้ทีหลังจากหน้าโปรไฟล์เสมอ
  - **แยกหน้า `users.php` (list + เพิ่ม) ออกจาก `profile.php` (แก้โปรไฟล์)** ตามที่ตัดสินใจกัน (งานคนละแบบ แยกหน้าทำงานชัดเจน) — `profile.php` รับทั้ง 2 เคส: ไม่มี `?id=` (หรือ id ตัวเอง) = แก้โปรไฟล์ตัวเอง เปิดให้ staff ทุกคน (`requireLogin()` เฉยๆ), มี `?id=` ของคนอื่น = ต้อง `manage_users` (เช็คกันคนพิมพ์ URL เดา id) — ช่อง "สิทธิ์" ในฟอร์มโชว์เฉพาะคนมี `manage_users` เท่านั้น (`$canEditRole = userCan('manage_users')`) แม้แก้โปรไฟล์ตัวเอง editor/author ก็ไม่เห็นช่องนี้เลยเพราะเปลี่ยนสิทธิ์ตัวเองไม่ได้อยู่แล้ว — ลบบัญชียังอยู่ที่ `users.php` (list-level action) ไม่ย้ายไป `profile.php`
  - **Avatar** — `saveUserAvatar($id)` (`includes/users.php`) อัปโหลดเก็บที่ `uploads/staff/{id}.{ext}` (1 ไฟล์ต่อคนเสมอ อัปโหลดใหม่ลบของเก่าอัตโนมัติแม้เปลี่ยนนามสกุล — pattern เดียวกับโลโก้เว็บใน `settings.php`'s `saveSiteAsset()`) — เพิ่มเช็ค `mblog_staff.avatar_path` ใน `uploadPathInUse()` (`includes/uploads.php`) กัน `orphan-files.php` เข้าใจผิดว่า avatar เป็นไฟล์กำพร้าแล้วเสนอให้ลบทิ้ง (จุดเดียวกับที่เคยพลาดกับโลโก้เว็บมาก่อนหน้านี้ — ดูหัวข้อระบบกำจัดไฟล์กำพร้า)
  - **Avatar placeholder แบบ Gmail** — ยังไม่มีรูปโชว์ตัวอักษรแรกของชื่อ (`avatarInitial()`: `mb_strtoupper(mb_substr(userDisplayName($user), 0, 1))` — ตัวอักษรไทยไม่มีเคส ผ่านเฉยๆ) บนพื้นสีทึบ 1 ใน 10 สี คงที่ต่อคนเสมอ (`avatarColorClass($id)`: `'avatar-color-' . ($id % 10)`, ใช้ `id` ไม่ใช่ email/username เพราะเป็นค่าเดียวที่ไม่เปลี่ยนแม้แก้โปรไฟล์ทีหลัง) — ชุดสี `--avatar-color-0` ถึง `-9` ใหม่ใน `base.css` แยกจากชุด `--tag-*` เดิมโดยเจตนา (tag เป็นโทนพาสเทล+ตัวอักษรเข้ม ออกแบบให้เข้ากับพื้นการ์ด ส่วน avatar ต้องเป็นพื้นทึบ+ตัวอักษรขาวเสมอ คนละโจทย์คอนทราสต์) มี override ธีมมืด (เข้ม/หม่นลงกว่าธีมสว่าง กันจ้าเกินไปบนพื้นมืด) — ขนาด 3 แบบผ่าน class เสริม: `.avatar-thumb` (32px, ตาราง `users.php`), `.avatar-thumb-md` (45px, topbar), `.avatar-thumb-lg` (64px, `profile.php`) ตัวอักษรปรับ font-size ตามขนาดจริงที่ทดสอบแล้ว (18px/28px/45px ตามลำดับ)
  - **Badge สิทธิ์มีสีแยกตาม role** — `users.php`: ใช้ชุดสี `.category-tag-*` เดิม (ระบบเดียวกับ badge หมวดหมู่บทความ) แทนที่จะสร้างสีใหม่ — admin = ม่วง, editor = ฟ้า, author = เขียว (`$roleColors` array ใน `users.php`)
  - **Topbar account menu เป็นค่าเริ่มต้นทั้งเว็บ ไม่ใช่แค่โซนแอดมิน** — ก่อนหน้านี้ `adminTopbarActions(array $extraLinks = [])` ใน `includes/admin-nav.php` (โหลดเฉพาะหน้าแอดมิน) แต่ละหน้าต้อง `$topbarActions = adminTopbarActions([...])` เองแล้วส่งเข้า `render_header()` — ย้าย logic ทั้งหมดเป็น `topbarAccountMenu()` ใน `includes/auth.php` แทน (ไฟล์นี้ถูก `config.php` require ทุกหน้าอยู่แล้วไม่ว่าหน้า public หรือแอดมิน) แล้วให้ `partials/header.php` เรียก `<?= topbarAccountMenu() ?>` ตรงๆ ไม่ต้องรอ `$topbarActions` จากหน้าไหนอีก — ลบ `adminTopbarActions()` ทิ้งทั้งฟังก์ชัน, ไล่ลบ `$topbarActions = ...;` + argument ที่เคยส่งเข้ามาออกจากทุกไฟล์ที่เคยเรียก (11 หน้าแอดมินที่เคยส่ง `$extraLinks`, 8 หน้า public ที่เคย set ตรงๆ เป็น string เช่น `'<a href="editor.php">+ เขียนบทความใหม่</a>'` แบบไม่เช็คสิทธิ์เลยด้วยซ้ำ)
    - **ผลข้างเคียงที่ตั้งใจ**: ปุ่ม "+ เขียนบทความใหม่"/"แก้ไข" ที่เคยโชว์แบบไม่เช็ค login เลยบนหน้า public (`articles.php`/`category.php`/`tag.php`/`search.php`/`feed.php`/`page.php`/`pages.php`/`article.php`) หายไป แทนที่ด้วย avatar เดียวกันทั้งเว็บ — เนื้อหาเดิมบางส่วน (เขียนบทความ) ย้ายกลับเข้าไปอยู่ใน dropdown ของ avatar แทน (ดูข้อถัดไป) เพื่อไม่ให้ฟีเจอร์หายไปเฉยๆ
    - **เมนู dropdown** (`.topbar-account-menu`/`.topbar-account-toggle`/`.topbar-account-dropdown` ใน `layout.css`) — คลิก avatar ไปหน้าโปรไฟล์ตรง, hover ค้างกางเมนู: "เขียนบทความ" (เช็ค `userCan('edit_articles')`), "จัดการเว็บ" (เช็ค `userCan('manage_settings')` แทนการเทียบ `role === 'admin'` ตรงๆ ตามหลัก capability-based — เผื่อ admin เท่านั้นเห็น), "โปรไฟล์", "ออกจากระบบ" เสมอ — ใช้กลไก `:hover`/`:focus-within` เดียวกับเมนูย่อย desktop (`.menu-item-has-children`/`.submenu`) ที่มีอยู่แล้ว ไม่ต้องเขียน JS ใหม่เลย แค่ anchor ขวาแทนซ้าย (`right:0` ไม่ใช่ `left:0`) เพราะอยู่ริมขวาสุดของ topbar
    - **ยังไม่ login เห็นปุ่ม "เข้าสู่ระบบ" แทน avatar** (`.topbar-login-btn`) — ใช้ redirect-back pattern เดียวกับ `requireLogin()` (`login.php?redirect=<current URI>`) กดจากหน้าไหนก็กลับมาหน้าเดิมหลัง login สำเร็จ
    - **"← จัดการเว็บ" เดิมที่เคยอยู่ topbar ทุกหน้าแอดมิน ย้ายไปอยู่บนสุดของ sidebar แอดมินถาวรแทน** (`partials/admin-sidebar.php`, จุดเดียวไม่ซ้ำทุกหน้า) — เช็คแล้วก่อนตัดสินใจย้าย: ไม่มีทางอื่นกลับไป `admin.php` เลยถ้าตัด topbar ทิ้งหมด (โลโก้แบรนด์มุมซ้ายบนลิงก์ไป `index.php` หน้าแรกของเว็บ ไม่ใช่ `admin.php`)
  - **บั๊ก CSS specificity 2 ตัวที่เจอและแก้ระหว่างทำ (รูปแบบเดียวกันทั้งคู่)** — สาเหตุร่วม: element ใหม่ที่เพิ่มเข้า `.topbar .actions` ใช้แค่ 1 class ในการให้สไตล์ ชนกับ rule เดิมของ topbar ที่จำเพาะกว่า (class+tag) เลยแพ้เงียบๆ ไม่มี error ให้เห็น ต้องเปิด browser devtools/`getComputedStyle()` เช็คค่าจริงถึงจะเจอ
    1. **`.topbar-account-menu` (div ใหม่) ตกไปอยู่คนละบรรทัดกับไอคอนค้นหา/ธีม** — `.topbar .actions` ไม่ใช่ flex container (comment เดิมในโค้ดบอกไว้แล้วว่าเป็น "normal inline flow") ลูกๆ ต้องเป็น inline-level เอง — `<a>`/`<button>` เดิมเป็น inline โดย default อยู่แล้วไม่มีปัญหา แต่ `<div>` ใหม่เป็น block-level default เลยตกบรรทัด แก้ด้วย `display: inline-flex; vertical-align: middle` แบบเดียวกับที่ `.site-search`/`.theme-toggle` ทำไว้อยู่แล้ว (ตามคอมเมนต์เดิมในไฟล์)
    2. **ปุ่ม "เข้าสู่ระบบ" ตัวอักษรสีเข้มผิดสีทุกธีม** (ควรเป็นสีขาวบนพื้น `--color-primary`) — `.topbar a { color: var(--topbar-text); }` (1 class + 1 tag = specificity 0,1,1) ชนะ `.topbar-login-btn { color: var(--text-on-primary); }` (1 class = 0,1,0) เพราะจำเพาะกว่า แม้ `--text-on-primary` เองจะมีค่าถูกต้อง (ขาว) อยู่แล้ว ก็ไม่มีผลเพราะแพ้ specificity ไปตั้งแต่แรก — แก้ด้วยเปลี่ยน selector เป็น `.topbar a.topbar-login-btn` (2 class + 1 tag) ให้จำเพาะกว่า `.topbar a` — เจอวิธีวินิจฉัยจริงคือเช็ค `getComputedStyle()` ในเบราว์เซอร์เทียบค่าที่ตั้งใจ ไม่ใช่แค่อ่านโค้ด CSS เฉยๆ (ค่าตัวแปร `--text-on-primary`/`--color-primary` ที่อ่านได้ตรงกับที่ตั้งใจ แต่ค่าที่ apply จริงบน element ไม่ตรง — ชี้ไปที่ specificity ทันที)
  - **โครงสร้างสีธีม custom ผ่าน `theme-editor.php` มีผลกับ debugging** — ระหว่างไล่บั๊กข้อ 2 ด้านบน พบว่าเว็บนี้ปรับสีหลัก (`--color-primary`) จากส้ม/โคอรัลเริ่มต้นของ `base.css` เป็นน้ำเงินกำหนดเองผ่าน `theme-editor.php` แล้ว (`custom_theme_colors` ใน `mblog_settings`) — เวลา debug สีจริงต้องเช็คค่าที่ render จริงในเบราว์เซอร์ (`getComputedStyle`) เสมอ ไม่ใช่เดาจาก hex ใน `base.css` เพราะเว็บนี้อาจ override ไว้แล้วโดยไม่รู้ตัว
  - **เพิ่มคอมเมนต์กันลืมในโค้ด 2 จุดที่เช็ค role ตรงๆ** — `users.php`/`profile.php` (ดูข้อ capability-based ด้านบน) อธิบายเหตุผลที่เป็นข้อยกเว้น + เตือนว่าถ้าเปลี่ยนโครงสร้าง role ใหญ่ๆ ในอนาคต (เช่นเปลี่ยนชื่อ role/ย้าย `'*'` ไป role อื่น) ต้องมาแก้ 2 จุดนี้เองด้วยมือ ไม่อัปเดตตามอัตโนมัติเหมือนจุดอื่นที่ผ่าน `userCan()`
  - **`theme-editor.php`: คอลัมน์ label ในตารางโหมดขั้นสูง (`.te-adv-row`/`.te-adv-head`) แคบลงจาก `130px` เหลือ `105px`** — วัดความกว้างจริงของ label ที่ยาวที่สุด ("ตัวหนังสือบนสีหลัก") ด้วย Canvas `measureText()` ที่ font จริงของหน้า (~100px) แล้วเผื่อ margin เล็กน้อย พื้นที่ที่ประหยัดได้ย้ายไปให้กล่องกรอก hex 2 คอลัมน์ (เดิมค่า hex ยาวๆ เช่น `#a9583e` ถูกตัดจนอ่านไม่ครบ)
